# M4 本地构建 / CCR 发布脚本检查记录

日期：2026-09-09。本节覆盖新增发布流程；下方为此前固定基线包的历史检查，不能视为新镜像的构建或推送结果。

- 发布脚本：`m4/deploy/build-and-push.sh`，参照 M3 默认 CCR 仓库，固定 linux/amd64，使用 M4 专属版本及提交标签。仅接受干净 Git 工作区，导出 Git 快照构建，生成同一快照的 HTML、Flow 和部署包。
- 6 项脚本测试通过：正常构建/标记/推送/manifest 调用顺序、SHA 与镜像元数据及 ZIP 校验、拒绝未提交源码、拒绝 M3 风格版本标签、错误架构/构建失败禁止推送、提交标签推送失败立即停止。测试使用真实临时 Git 仓库与模拟 Docker CLI，未连接镜像仓库。
- 当前工作区 16 项网关离线测试通过（包含已存在的账单路由断言）。Bash 语法、Python 语法和差异空白检查通过。
- 生产 Compose 解析通过：无 build 字段，镜像取自 CCR，平台 linux/amd64；宿主端口 8844/8845 均只绑定127.0.0.1、容器目标仍8844。
- 尚未实际执行本次镜像构建或 CCR 推送。工作区含待提交的部署与账单修改；提交预定发布内容后运行脚本。没有更新旧 ZIP，也没有将旧包声明为新镜像配套包。正式发布版本以脚本输出的 `release.json` 和对应镜像为准。
- 未操作生产、未导入 Node-RED Flow、未访问真实数据源或触发求解/下发。

---

# 此前 TCP 固定基线部署包检查（历史记录）

日期：2026-09-09。目标平台：**Linux AMD64**。安装目录：`/userdata/holo/pyfiles/m4`。

本版按用户最新决定恢复 TCP。默认仅发布 `127.0.0.1:8844`，宿主端口可通过 `M4_BIND_PORT` 修改，容器内部仍为 8844。Node-RED 使用内置 HTTP Request，保留 iframe Token 校验，不新增 Nginx。

| 项目 | 本次实际结果 |
|---|---|
| 发布基线 | 业务源码及 HTML 取自提交 `620ef58`，叠加本次 TCP 与生产目录配置；不包含工作区其他未提交功能 |
| 发布版网关离线检查 | 15 项通过，覆盖 Token、Origin、接口/路径限制、分页、请求体、响应处理和生成 Flow 连线；临时检查目录显式指定系统 Python 后通过 |
| 工作区兼容检查 | 16 项网关检查通过，包含工作区独立账单功能的额外断言；账单代码不进入本发布包 |
| Compose 配置 | `docker compose config` 通过；默认端口 8844 与替代端口 8845 均核对为本机地址绑定、容器目标 8844，平台固定 linux/amd64 |
| 部署脚本 | Python 语法检查、Git 差异空白检查通过 |
| 交付文件 | 校验 SHA256SUMS、ZIP 内容、HTML 与 Flow Template 一致；无 Socket 专用脚本，无真实凭据、运行数据或工作区账单文件 |

Dockerfile、entrypoint.py、healthcheck.py、preflight.py 恢复为此前已完成 AMD64 镜像构建和隔离运行检查的 TCP 实现。本次核对这些文件与原 TCP 交付一致，未重复构建镜像或运行容器。前端与业务源码沿用已提交版本，本次未重复浏览器或求解器回归。

按用户要求，**没有启动 Node-RED 或导入 Flow 验证，没有连接生产机器**，没有读取真实上游、启动真实求解或下发设备指令。现场端口占用、Node-RED 网络/环境变量、HTTPS/iframe 响应头、真实数据源权限/连通以及生产性能仍需现场验证。

ZIP 为源码部署包，含 HTML、正式部署 Flow、Dockerfile、Compose、后端源码和手册，不包含 Docker 镜像 tar。Node-RED 为宿主机进程时使用 `http://127.0.0.1:8844`；作为容器时按手册接入共享网络并使用 `http://m4-api:8844`。旧 Socket 发布包保留为历史材料，本次应使用文件名含 `tcp-userdata` 的发布包。
