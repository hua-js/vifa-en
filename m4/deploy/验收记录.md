# M4 Socket 部署包检查记录

日期：2026-09-09。目标平台：**Linux AMD64**。本记录覆盖本地材料及隔离容器检查，不代表现场部署完成。

| 项目 | 本次实际结果 |
|---|---|
| AMD64 构建 | `docker build --platform linux/amd64` 成功；Python 3.12.14、Pyomo 6.10.1、HiGHS 1.15.1，沿用 22 个已锁定依赖 |
| 无后端 TCP 端口 | Dockerfile 无 EXPOSE，Compose 无 ports；无网络测试容器的 `/proc/net/tcp` 与 `/proc/net/tcp6` 均无监听条目 |
| 应用与 Socket 权限 | 应用 UID/GID 10001、有效 capabilities 为零；Socket 归属 10001:10001、权限 0660；应用单进程运行 |
| 共享目录访问 | 独立客户端以 UID 1000 / GID 10001、只读挂载 Socket 目录、无网络，成功进行 GET、本地偏好 PUT，以及应返回 422 的无效 POST；未启动求解 |
| 组权限隔离 | UID/GID 1000:1000 的测试进程不能访问 Socket，返回 PermissionError |
| 运行前检查与健康 | Socket 版 preflight.py、healthcheck.py 通过；不访问上游，不计算计划 |
| 重启 | 测试容器重启后 Socket 健康检查通过，已保存的本地偏好 revision=1 保留 |
| Socket 生命周期 | `tests/test_m4_deploy_socket.py` 5 项通过：残留 Socket 清理、并发锁、拒绝覆盖活动 Socket、普通文件及符号链接保护 |
| Node-RED 脚本离线检查 | `node tests/m4_node_red_gateway_test.js` 15 组通过，覆盖 Token、Origin、接口与路径限制、分页、请求体、响应处理和生成 Flow 连线 |
| 原生 Socket 转发 | `node tests/m4_socket_proxy_test.js` 通过，覆盖 GET/PUT/POST、请求头隔离、不跟随重定向/重试、连接失败和模块缺失；使用原生 Node.js，没有导入 Flow |
| 前端 | HTML 未因 Socket 改造而修改；沿用先前已通过的 iframe Token Mock 浏览器回归 |
| 文件 | 部署 Python 语法、差异空白检查通过；发布包附 SHA256SUMS，HTML 与 Flow Template 一致 |

检查使用本机 Docker 的 AMD64 兼容运行环境、假 Token、独立空白业务卷及 Socket 卷。没有读取真实上游、调用真实求解或下发设备指令。临时测试容器及专用卷在检查后清理。

按用户要求，不启动 Node-RED 做 Flow 导入验证，不连接生产机器。尚未验证现场 Node-RED settings.js 合并、服务用户组、宿主机目录权限、HTTPS/iframe 响应头、真实数据源权限/连通、现场迁移与备份恢复、生产机器性能及长期稳定性。

ZIP 是源码部署包，含 HTML、配套 Flow、Dockerfile、Compose、后端源码和手册，不包含 Docker 镜像 tar、真实凭据、Socket 文件或运行记录。Node-RED 和后端须同机共享 Socket 目录；页面 HTTPS 入口仍沿用现有部署。
