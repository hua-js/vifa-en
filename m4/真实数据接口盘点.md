# M4 真实数据接口盘点

核查日期：2026-09-07；依据：现有 M1/M2 代码、用户指定 VIFA 网站已登录页面、对同站点的只读 GET 请求。没有修改远端数据、配置或设备状态，没有使用 SSH。鉴权复用已有本地配置，不在本文记录凭据。

## 真实候选计算（2026-09-07 21:02 更新）

用户最新范围为“AI 接入能力待定，启动候选计算即可”。本地已接通 POST/GET `m4-api/stations/{station_id}/candidates`：服务端重新读取真实输入，以当时参与柜能力调用现有优化器，返回配套输入、三类候选及版本快照。未接 AI、滚动调度或真实计划下发，账单继续暂缓。

- 21:02:14 电站 1 实算成功，窗口为 09-07 21:15 至 09-08 21:15；参与 emu12、排除 emu11，可用容量 522 kWh、充/放上限各 100 kW、初始 SOC 2.8%。
- 三候选均为 `optimal`，每类 96 点，单类求解约 0.10 秒。均衡购电成本 5329.28 元，节费与光伏优先均为 5317.30 元；同输入无储能购电基线 5824.64 元。站 1 没有光伏，后两类计划一致；这些仅是当前预测及人工参数下的计算结果，不代表设备执行收益。
- 使用独立版本化候选调试模板，目标顺序沿用已确认口径，尺度和容差随结果可追溯；没有把演示结果作为现场候选，也没有调整已保存的真实参数。
- 首次 21:00:26 取数中 emu12 源时间晚于本机约 67 毫秒，按原校验排除而未求解；重新取样后通过。保留时间检查，未放宽状态或 SOC 边界。后续页面按当前采样判断，不承诺旧样本永久可用。
- 相关后端 77 项测试通过，含真实优化器、按柜能力、版本变更、过期、重复计算和失败清旧结果；独立审查还验证两站同时真实求解各 3 个最优候选、缓存互不覆盖。候选、输入、参数、离线查看四组浏览器回归通过。
- 21:11:02 用户当前本地页面实际点击计算再次成功，3类均可用；输入依据中已核对求解时间、参与范围、归一化尺度、权重及容差。两站四种宽度明暗实际渲染通过，站2仍未配置且本次未来窗口缺85个负荷点。

## 当前规则：异常柜隔离（2026-09-07 20:41 记录）

用户明确修改此前全站阻断规则：有柜子告警时，不能影响其余正常柜的充放电计划。当前保留告警字段读取映射，但异常柜单独排除；正常柜继续，只有全柜不可用或必需站级输入不完整才阻断。

- 原固定柜名单及每柜配置容量不变；可用容量/最大充放电功率按参与柜数占原柜数的比例折算。功率等分是当前人工配置折算方法，非 EMS 动态能力。
- 计划 SOC 仅按参与柜加权。完整全站 SOC/上游站级 SOC 单列对照，不能把两种范围混用。逐柜告警/未知/缺失/重复/过期均局部隔离；参与名单及政策进入版本。
- 20:41:07 的只读结果：电站 1 `status=ready`、`participation_status=partial`，参与 emu12、排除 emu11；原配置 1044 kWh，可用 522 kWh；最大充/放 100/100 kW；参与 SOC 2.8%，全站 SOC 2.4%。未来负荷、电价、光伏均 96 点。
- 真实浏览器窗口 20:45 至次日 20:45：电站 1 已显示部分柜参与；电站 2 仍待配置且缺次日零点后的 83 个负荷点。
- 当时 60 项相关后端测试通过，覆盖真实优化器的剩余功率限幅；部分参与、全部不可用、两站切换、源版本与过期刷新通过浏览器核验。候选计算已由上方最新接入完成，设备下发仍不在本阶段。

## 此前第一次接入结果（按柜规则已由上文覆盖）

本节保留第一次接入时的观察；当前隔离规则以上文及《调度参数配置说明》为准。

- 用户已确认防逆流没有方向控制，40 kW 当前无实际作用。只显示“未启用”，不形成最小购电、允许反送或其他优化约束；需量直接用 need_kw，免调功率只作来源展示。
- 用户确认逐柜可用性和全站阻断规则：待机/充电/放电且无故障告警才参与；任何一柜异常、未知、缺失或过期，本站本轮不生成新候选。
- 用户确认末段电价延续至 24:00；两站共用。总容量按固定柜数平分后做 SOC 容量加权，排除 emu27。
- 已新增本地 `GET /m4-api/stations/{station_id}/inputs`。读取 M3、七日光伏历史、共用电价、控制来源及柜级实时状态，生成北京时间连续 96 点并展示来源完整性和阻断原因。
- M3 直接复用已持久化的 `energy_forecast_manual_runs/energy_forecast_manual_points`，只读成功/已评估的 900 秒任务及 `station_total_load`。页面“最新任务”偏向覆盖今天，M4 需查询覆盖整个未来窗口的现有任务，按每点的最新完成时间选取，不改 M3 页面或创建新任务。
- 首次端到端读取发现 M3 numeric 字段返回小数字符串，已兼容合法有限非负值；不接受空白、布尔值、NaN、Infinity。站 1 今日和明日现有任务可拼齐 96 点；站 2 当前仅覆盖今天，首个缺点为 **2026-09-08 00:00 北京时间**，不能复制今日补齐。
- ES02 的 08-31～09-06 七日数据已通过适配层分钟覆盖检查，历史同时段中位数参考可形成未来 96 点。两站电价均覆盖 96 点，ES01 光伏为 0。历史参考不是天气预测，不提供精度/收益保证。
- 最终浏览器实读窗口为 09-07 20:30 至 09-08 20:30；ES01 负荷 96/96，ES02 负荷 14/96（缺 82 点），两站电价和光伏输入均为 96/96。
- 当前本地站 1 参数已保存，柜级 SOC 加权为 2.4%，emu11 存在活动告警，因此全站阻断；站 2 尚未保存本地参数，仍展示固定六柜原始 SOC，但不计算可用于调度的加权初始值。这些是本次读取状态，后续以刷新结果为准。
- 本轮只完成输入读取、校验及请求构造，未启动现场候选计算、AI 选择或 EMS 下发；账单继续暂缓。下一步补齐站 2 参数及未来负荷预测、让参与状态通过校验后，接入真实候选计算与展示。

本轮验证：107 项后端单元/集成测试通过；参数、真实输入、原演示与显式离线入口的四组浏览器回归通过；实际两站只读页面完成 1440/768/390/320px 明暗模式检查，无页面横向溢出，柜表在窄屏区域内横向滚动。关键请求与来源规则经过独立审查并修复数值字符串、过期请求和版本竞争问题。未修改真实参数数据库或生产数据。

## 已验证：光伏实时与历史

基础地址：`https://vifa.hlszh.com/api`。

| 用途 | 接口 | 标识、时间与字段 | 核查结论 |
|---|---|---|---|
| 电站 2 实时光伏 | GET `/t_emu:list` | `f_es_sn=ES02`、`emu_sn=emu27`；`last_time_iso`；`latest_solar_power`（交流功率）、`input_ac_solar_power`（输入侧功率） | 2026-09-07 16:15:02 北京时间样本：交流 77.679 kW、输入侧 80.5819 kW；单位沿用已实现的 M2 字段约定 |
| 电站 2 历史光伏 | GET `/t_es_data:list` | `es_sn=ES02`；`timestamp`；`ac_solar_power` | 2026-09-07 16:15:30 样本为 76.6 kW；同一记录 `solar_power=0`，不能以该字段替代交流光伏 |
| 历史负荷与储能 SOC | GET `/t_es_data:list` | `load_power`、`emus_soc`；同一时间/电站标识 | 字段已在实际响应中存在，负荷为数值字符串；此处不是未来预测，也不证明聚合 SOC 满足 M4 可调度能力定义 |

实时和历史采样时间不同，上述样本只能确认字段存在、量级接近，不构成逐点同源性证明。正式接入仍需核对采样聚合与实时值写入历史的口径。

M2 当前拓扑将 ES01 标记为无光伏，ES02 光伏表为 emu27；M1 的光伏展示另由九台 Growatt 逆变器 a35 汇总。M4 优先调查本站点 emu27 与站级历史链路，避免把两种采样时间或来源混合。现场拓扑发生变化时需要更新映射。

查询参数使用 `filter` JSON、`fields`、`sort`、`page`、`pageSize`。历史范围采用起点包含、终点不包含，例如：

```json
{
  "$and": [
    {"es_sn": {"$eq": "ES02"}},
    {"timestamp": {
      "$gte": "2026-08-31T00:00:00+08:00",
      "$lt": "2026-09-07T00:00:00+08:00"
    }}
  ]
}
```

`t_es_data` 当前响应不含 `id`；`t_emu` 设备编号字段为 `emu_sn`，不是 `sn`。分页按响应的 `meta.hasNext` 继续。

### 七天历史实查

共 17,455 条；起止北京时间为 2026-08-31 00:00:00 至 2026-09-06 23:59:30。相邻间隔为 30 或 60 秒，不能一律按 30 秒缺点判定。

| 日期 | 有效记录 | 非零记录 | 最大交流功率 kW | 有有效值的 15 分钟时段 |
|---|---:|---:|---:|---:|
| 08-31 | 1,440 | 715 | 517.8 | 96 / 96 |
| 09-01 | 1,615 | 740 | 271.0 | 96 / 96 |
| 09-02 | 2,880 | 1,472 | 517.2 | 96 / 96 |
| 09-03 | 2,880 | 1,438 | 516.4 | 96 / 96 |
| 09-04 | 2,880 | 1,464 | 497.6 | 96 / 96 |
| 09-05 | 2,880 | 1,488 | 428.2 | 96 / 96 |
| 09-06 | 2,880 | 1,445 | 242.6 | 96 / 96 |

这证明历史来源和分时段覆盖可用，不证明天气代表性、未来精度或完整质量验收。历史基线的重采样、有效日判断、实时修正和异常处理仍需在 M4 输入适配层设计实现；不得在优化核心里猜测或填补 96 点输入，也不把历史记录本身当作预测结果。

## 已验证：电价配置

页面：`https://vifa.hlszh.com/admin/dl0v5vt2osc`。页面中文标签与实际接口字段已核对。

- GET `/t_peak_diy:list`：24 行，字段 `start_time`、`end_time`、`period_type`；`gu`=谷、`ping`=平、`feng`=峰。
- GET `/t_rate:list`：当前返回一条配置（id=3），`hprice`=峰段 1.10016875、`fprice`=平段 0.65856875、`vprice`=谷段 0.26736875，单位元/kWh；`jprice` 与 `shengu` 为 0，当前时段表未引用。
- 当前时段：谷 00–08；平 08–10、12–14、19–23:59；峰 10–12、14–19。
- 最后一行是 `23:00:00 → 23:59:00`，不是 24:00。接入前须核对既有计价程序对末分钟和右端点的处理，不能未经确认悄悄补齐。
- 当前返回未见电站/生效日期字段；用户已确认两站共用当前电价。历史回放时的有效版本来源仍待核实，售电价未发现。

## 早期盘点边界（当前进展见上方最新条目）

- 用户确认目前没有光伏预测；已有历史与实时数据可作为建立调度参考基线的输入，基线尚未实现。
- M3 负荷预测接口继续复用现有实现，但滚动未来 24 小时覆盖、发布时刻、缺失处理需在接入时验证。
- 已发现柜级 `latest_soc`、装机功率/容量和状态等字段；静态额定参数不能直接等同 EMS 当前站级最大可充放功率、可用容量或可用性。M4 的正式接口仍需要站级可调度能力口径。
- 需量上限、SOC 硬边界和推荐范围、效率、反送约束及正式 EMS 接收/回读字段本轮尚未核查。
- 账单抽屉仍暂缓；没有真实下发授权，不执行设备控制。

## 已验证：需量、防逆流与每日计划（2026-09-07 补充）

用户提供三张表并确认：`t_model.kw` 为电站总功率，每日重复；需量表中文列为变压器额定容量 kVA、需量阈值 kW、免调功率 kW；`re_kw` 为“防逆流限制功率(kW)”。只读接口值如下：

| 接口 | 电站标识 | 字段 | ES01 | ES02 |
|---|---|---|---:|---:|
| `/t_need:list` | `f_es_sn` | `need_kw` 需量阈值 kW | 1062.5 | 504 |
| 同上 | 同上 | `reserved_kw` 免调功率 kW | 50 | 5 |
| 同上 | 同上 | `rated_capacity` 变压器 kVA | 1250 | 630 |
| 同上 | 同上 | `load_rate` 配置系数 | 0.85 | 0.8 |
| `/re_flow:list` | `fk_es_sn` | `re_kw` 防逆流限制功率 kW | 40 | 40 |

`need_kw` 保留原精度，电站 1 页面 1063 为显示取整，不修改为 1063，也不扣免调功率。额定容量是 kVA，不能替代独立购电 kW 硬上限。响应中未发现明确启停字段，不能由记录存在推断设备控制已启用。

`/t_model:list` 返回 8 行，`es_sn` 是数组，两站各 4 行；字段 `start_time`、`end_time`、`type`（charge/discharge）、`kw`、`repeat=每天重复`。

| 每日时段 | 动作 | ES01 站级 kW | ES02 站级 kW |
|---|---|---:|---:|
| 00:00–08:00 | 充电 | 100 | 100 |
| 08:00–12:00 | 放电 | 60 | 90 |
| 14:00–18:00 | 放电 | 60 | 90 |
| 19:00–21:00 | 放电 | 60 | 90 |

空档未配置，不推定停机或保持上一指令。该计划不是实际执行回读，也不是设备最大能力快照。

已完成三表到本地 `m4-api/stations/{station_id}/control-sources` 与参数抽屉的只读链路，移除 3 个重复手填字段。防逆流字段名称仍不足以确定“最小正向购电”或“逆流限制/触发”的准确规则；当前标记待确认，不填入优化器外送上限。此前“需量来源未找到”的缺口已解决；SOC/动态能力与正式 EMS 下发/回读字段仍待接入。没有修改生产表或设备状态。


## M1 状态与柜级 / 站级 SOC 核查（2026-09-07）

用户补充：M4 状态参考 M1；两站共用电价；ES01 无光伏、ES02 有光伏。以上作为接入约定，不再重复询问。

### M1 可复用部分

- 柜体范围沿用 `m1/dashboard_energy_api.py`：ES01 为 emu11、emu12；ES02 为 emu21～emu26。emu27 是光伏表，不能进入储能 SOC、容量或可用状态汇总。
- M1 的运行状态来自 `en_realtime.run_status`，按 `fk_en → en.id` 关联柜体，缺失时页面回退 `en.run_status`。SOC 为 `en_realtime.soc` 直接透传；M1 没有实现站级 SOC 平均或容量加权。
- M1 显示枚举：`wait/standby` 待机、`charge` 充电、`discharge` 放电、`stop` 停机、`offline` 离线、`fault` 故障、`debug` 调试、`alert` 待确定、`exception` 异常；`generate/dc_charge/work/reverse` 还适用于其他能源节点，不能全部变成储能可调度白名单。
- M1 `nodeOnline` 检查数据质量和 60 秒内的 `timestamp`；`siteOnline` 只要求任意一个节点新鲜。它们描述数据在线，不是 EMS 的充放电许可或全站储能健康判断。
- 单柜采集时间由 `t_emu.last_time_iso` 提供，M1 作为 `cabinet_timestamp` 单独返回。M4 接入应保留各柜源时间，检查全部目标柜，不能用最新一柜的时间掩盖其他柜过期，也不回退静态状态来证明实时可用。
- M1 原始业务表基址为部署内的 `http://holobase/api`。在用户指定的 `https://vifa.hlszh.com/api` 只读查询 `es:list`、`en:list`、`en_realtime:list` 均返回 404，故本轮不能声称已在该域接通 M1 原表；已可访问的是下述 `t_emu` / `t_es_data` 链路。未尝试连接其他远程主机。

代码证据：`m1/dashboard_energy_api.py:255`（节点身份）、`:289`（SOC/状态读取）、`:552`（柜体源时间）；`m1/dashboard_energy.html:731`（状态枚举）、`:767`（在线）、`:773`（场站在线）、`:1104`（逐节点 SOC 显示）。

### 真实接口同时有两种粒度

只读查询时间为 2026-09-07 19:32:40 北京时间。柜体源时间为 19:32:38～40，站级最新记录为 19:32:00；两次请求不是同一原子快照。

| 粒度 | 接口 / 字段 | ES01 本次样本 | ES02 本次样本 |
|---|---|---|---|
| 柜级 SOC | `t_emu.latest_soc`，身份 `f_es_sn + emu_sn` | emu11=2%、emu12=2.8% | emu21=2%、emu22=1.95%、emu23～26=2% |
| 站级 SOC | `t_es_data.emus_soc`，身份 `es_sn` | 2.4% | 1.992% |
| 柜级状态 | `t_emu.emu_status` | 两柜均 `wait` | 六柜均 `wait` |
| 部件状态 | `bcu1_status/bcu2_status/pcs1_status/pcs2_status` | 均 `wait` | 均 `wait` |
| 告警状态 | `t_emu.alert_status` | emu11=`alert`、emu12=null | 六柜均 null |
| 柜级容量 / 功率 | `emu_power_storage/emu_installed_power` | 均 null | 均 null |

`soc1/soc2` 本次均为 null，应读取有效的 `latest_soc`。emu27 的 SOC 和储能状态为空，符合光伏表身份，必须排除。

ES01 的简单平均为 `(2 + 2.8) / 2 = 2.4%`；ES02 为 `(2 + 1.95 + 2 + 2 + 2 + 2) / 6 ≈ 1.992%`，与站级字段本次吻合。由于各柜容量为空、采样不同步，且未读取到站级写入算法，只能确认“样本与简单平均一致”，不能证明所有时段采用简单平均或容量加权；同容量时两种算法本来就相同。

### M4 输入适配约定与边界

1. 已找到真实 SOC 来源，无需再索取新的 SOC 接口。柜级值与上游站级字段保留各自来源和采样时间。用户后续确认 M4 按下述“配置容量等分后加权”生成自己的站级 SOC；上游 `emus_soc` 保留作对照，不声称已核实其写入算法。
2. 用户已确认：M4 使用本站配置 `energy_capacity_kwh`，按固定储能柜数等分单柜容量，再按容量加权 SOC。ES01 为 2 柜、ES02 为 6 柜；emu27 排除。公式为 `C_i = C_station / N`，`SOC_station = sum(C_i × latest_soc_i) / sum(C_i)`，等容量时数值等于简单平均。来源必须标注“按配置容量等分后加权”，不是实测单柜容量或动态可用容量。各柜 SOC/时间须齐全有效；缺柜时不能缩小分母后宣称全站聚合完整。该口径已确认，完整实时适配尚未实现。
3. 可复用 M1 状态标签和柜体映射；真实可用性仍要结合全部柜的时效、运行/故障状态与 EMS 许可。当前接口没有经确认的站级 `available`、允许充/放或动态降额字段；`wait` 不能自动等于允许充放，null 告警不能直接解释为“无故障”。本次 emu11 同时有 `wait` 和 `alert`，尤其不能只取运行状态忽略告警。
4. 低 SOC 样本按真实值保留；若低于已配置安全下限，应报告请求校验失败，不抬高 SOC 或放宽边界。
5. 电价采用共用来源；ES01 光伏按已确认拓扑为零，ES02 使用交流光伏历史和实时源。末段电价 `23:59:00` 的结束语义仍待核实，不擅自补齐最后一分钟。

本轮完成源码核查、真实只读取样与接口口径记录，没有改动 M1/M4 控制算法、参数数据库或生产环境；预测/电价/实时状态的完整运行适配尚未完成。


### SOC 容量口径补充确认（2026-09-07）

用户明确“容量是按配置上的平分，可以说明是容量加权”。因此不再要求额外提供单柜容量来完成 M4 首版聚合。M4 使用配置分配权重与逐柜真实 SOC，保留聚合方法、站级配置版本、柜体名单及采样时间；上游 `t_es_data.emus_soc` 用于对照。原始接口容量字段为空与上游写入算法未证实的事实仍然保留，此确认定义的是 M4 的计算口径。
