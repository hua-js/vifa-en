# M4 真实候选计算实施计划

**目标：** 按用户确认，仅接通真实输入到三类数学候选的计算和预览；AI 接入待定，不实现设备下发。

**架构：** 本地 FastAPI 在服务端重新读取并校验输入，复用 `request_from_inputs`、数学优化器与 B1 编排契约；按站保留进程内最近结果和完整输入快照。页面复用现有候选展示，真实结果与演示来源保持明确。

**约束依据：** `CODEX_HANDOFF.md` 中已确认的目标优先级、按柜隔离和最新用户要求；数学核心与 B1 契约不变。账单暂缓；不修改生产、参数实值，不接 AI、EMS 或自动滚动任务。

## 1. 调试目标模板

- [x] 新建 `m4_settings/objectives.py` 和 `tests/test_m4_objectives.py`，返回按站可覆盖的三类版本化目标模板及调试元数据。
- [x] 固定需量峰值、持续量前两级；均衡随后 SOC、归一化费用/PV 综合、吞吐；费用及 PV 类遵守已确认优先级。返回独立副本，拒绝无效配置。

## 2. 候选服务与 API

- [x] 先在 `tests/test_m4_candidates.py` 复现未配置、输入不完整、版本变化、过期、重复计算、跨站隔离和求解失败的预期行为，再实现 `m4_settings/candidates.py`。
- [x] POST `/m4-api/stations/{station_id}/candidates` 只接受 `configuration_version`，真实源值由服务端读取。返回输入快照、优化请求、B1 结果、模板元数据和预览有效期。每站计算锁防重复，异常清除本轮旧结果。
- [x] 开始与完成时检查保存参数版本；完成时检查控制配置版本及输入时效。求解单候选上限 30 秒，三类合计约 90 秒，不增加后台任务。
- [x] GET 同路径返回进程内最近结果或 null；过期/版本变更明确标注失效，不伪装当前计划。
- [x] 用隔离数据库和固定真实形状输入调用真实优化器，验证 3 个候选、每个可用计划 96 点、能力和 SOC 边界、未选择/未下发状态。

## 3. 页面与验证

- [x] 修改线上 HTML，增加计算按钮、加载/失败/空/过期状态，按站缓存及请求代际隔离。支持三类点击预览及计划/SOC/费用/需量/光伏指标；AI 待接入。
- [x] 新增 `tests/m4_candidates_e2e.js`，验证点击计算、切站隔离、失败清空、旧响应拒绝和过期；回归原输入/参数/结果查看路径。
- [x] 独立审查业务边界；启动本地服务，实际计算站 1；检查明暗主题和桌面/平板/手机渲染，记录站 2 缺口。
- [x] 更新接口说明与交接文档。只报告实际通过的检查和计算结果，不宣称真实收益或执行效果。

## 验证记录

- 后端：`PYTHONPATH=tests .venv/bin/python -m unittest test_m4_realtime test_m4_settings test_m4_live_inputs test_m4_inputs_api test_m4_candidates test_m4_objectives -q`，77项通过。新增候选和目标模板均先红后绿。
- 浏览器：`m4_candidates_e2e.js`、`m4_live_inputs_e2e.js`、`m4_settings_e2e.js`、`m4_offline_console_e2e.js` 四组通过；候选包含控制刷新失败/旧响应、当前参与范围改变、过期历史、全候选失败和无光伏口径。
- 独立审查：无阻断项；两站并发真实HiGHS各返回3个optimal，缓存互不覆盖。
- 真实站1：21:02接口计算和21:11当前用户浏览器实际点击计算均成功，3类96点、emu12参与；站2本地未配置且次日预测缺85点。
- 真实页面两站1440/768/390/320明暗无页面横向溢出，已查看实际截图和最终输入依据抽屉；fixture截图 `/tmp/m4-candidate-shots-final`，真实截图 `/tmp/m4-candidates-final`。
- 没有调用AI、EMS或写生产，不修改已保存参数，没有提交或部署。
