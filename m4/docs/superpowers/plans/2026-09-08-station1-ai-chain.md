# 电站 1 真实输入到 AI 选择联调

**Goal:** 复用电站 1 真实读取与数学候选，接上 Qwen 候选摘要选择，输出可检查的全链路预览；如输入阻断则准确停在对应阶段。
**Architecture:** 使用现有 LiveInputService/CandidateService/LiveSelectionService。AI 单独读取精简候选摘要及已确认配置，返回候选 ID 与短理由；原始回答保留，程序核对候选和规则，模型超时/错选/输入变化不能生成可用选择。只读现场，不下发。
**Tech Stack:** 现有 Python/FastAPI、SciPy/HiGHS、Ollama HTTP、unittest。
**Spec:** 用户本轮要求及 CODEX_HANDOFF 的 station-1 profile_priority 规则。

## 约束与步骤

- [x] 真实数据诊断：获取 /inputs 并核对字段原值、预测覆盖；不得补假数据、忽略活动告警或把历史快照称为当前输入。当前证据：明日负荷缺点，两柜 PCS 状态未通过；进一步核对数据源。
- [x] 确定 AI 服务：用户已明确授权 e606.hlszh.com:729 的 qwen3.5:4b，并要求配置化。
- [x] 接口实现与 TDD：摘要不含 96 点计划或设备标识；严格 JSON 回答只可引用可用候选。按既有确定性选择作规则校验，记录正确/错选/服务失败/快照失效，失败不能回填成 AI 成功。模型严格串行且未知完成不自动重试。
- [x] 本地链路入口：读配置→获取数据→建模/求解→复验摘要→AI→返回后复核输入/候选/偏好。保留每阶段结果及可追溯来源；输入未通过则不求解、不调用 AI。
- [x] 执行相关测试与独立审查；用真实电站 1 联调，分别报告真实链路完成阶段及 Mock 验证覆盖。无法消除的上游缺口明确列出，不擅自生成上游预测或修改设备状态。
- [x] 更新短交接及运行说明，保留本地服务。

阶段证据：真实输入与三候选求解/复验已完成；用户补齐 M3 预测，PCS work 适配已修复。106 项相关测试通过，独立审查的进程中断门禁已复现/修复/复核。CLI 实跑停在 blocked_ai_configuration；远端地址确认仍待回复，未发送模型请求。

## 模型配置与实际调用（用户已授权）

用户明确同意沿用 e606.hlszh.com:729 的 qwen3.5:4b，并要求以后可更换。新增 `m4/ai_selection_config.json` 与严格配置加载模块，包含 enabled/provider/base_url/model/think/generation/读取超时/流预算；CLI `--ai-config`（或 M4_AI_CONFIG）指定文件，`--ollama-url`/`--model` 可覆盖，`--no-ai` 明确禁用。配置错字、非法类型/URL/超时均在取数前失败，不回退旧地址。每次运行固定一份配置并写入证据及哈希，串行/JSON复验/不下发规则固定。

先补替换模型、参数传播、优先级和非法配置的测试；移除 payload 与传输中的固定模型判断；验证后用用户授权服务重新跑真实链路。实际模型返回与复核状态独立报告，输入变化不可放宽。

配置化已完成：21项相关测试通过，独立审查预算问题已修复；真实模型完成一次（done/stop），正确选择balanced，耗时115.6秒，但post-AI实时复核409，未生成当前可用选择。未自动重试；需后续降低延迟，不能将此次记为实时验收通过。
