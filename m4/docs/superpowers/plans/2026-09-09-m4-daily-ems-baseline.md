## 2026-09-09 两站统一接口功率与容量

用户明确电站2同样使用单柜模式功率，容量来自t_es:list.es_power_storage。只读字段确认sn=ES01/ES02，整站容量1044/3132kWh。实施：两站模式表均按单柜解释；控制来源同时读取t_es正数容量并绑定版本；实时快照/部分柜能力、日基线、优化请求与页面统一使用接口整站容量，旧人工容量只保留历史兼容。缺失/重复/非法接口容量阻断新计算，不猜测或回退旧手填值。升级日基线版本使旧口径结果需重算。用户不测试要求持续有效。

## 当前实现：接口单柜功率与整站汇总（2026-09-09）

用户确认电站1每柜100/60kW来自充放模式表。当前读取t_model对应模式kw，不硬编码数值；同模式多行取最大值为该模式单柜上限，时段基线仍逐行采用原值。两柜全站汇总，实时仅按参与柜汇总，与较低人工配置取小值；电站2范围保持原口径。

此前把100/60kW当整站值导致基线少算一柜。日基线、滚动能力、快照复核及页面已接入新范围，源版本及EMS基线版本升级，旧日比较失效需重新生成。旧模拟金额和停止时刻均不得作为此版本结果。刚加入的固定硬上限方案已撤回，人工参数未改。

按用户再次要求，最终实现后未运行测试、语法检查、浏览器验收或新求解，仅加载本地代码。HTML与Flow模板已同步，未打包部署。下文保留历史过程，以本段为准。

---

## 2026-09-09 柜体功率硬上限补充

用户明确电站1每柜充电100kW、放电60kW；emu11/emu12两柜总上限200/120kW。实施：集中定义物理上限；保存参数及日/滚动请求入口校验；滚动按原参与柜数量折算，单柜不继承退出柜份额；参数页显示单柜/合计口径并阻止超限。保留较低人工配置及历史文件，不修改电站2，不涉及真实下发。沿用不跑测试套件要求，仅语法及本地只读查看。

# 2026-09-09 当前修正：基线需模拟有效充放电时长

用户指出充电可能提前充满、放电可能提前到 SOC 下限。旧版本直接展开时段额定功率不成立；下文上一轮结果仅为历史，273.25元差额不再作为新口径结论。

实施顺序（已完成代码与本地页面接入，未启动新优化或测试套件）：
1. 将时段表视为请求功率；按已有需量阈值优先限充/削峰，功率能力及 SOC 安全边界限制有效功率。
2. 在每个15分钟负荷/电价区间内计算到限所需时间，剩余时间待机；用实际模型时长积分费用，不把待机算为充放电。保留到限事件与控制原因。
3. 基线标记为 EMS 规则模拟，不能冒充现场执行遥测；若电量耗尽后预测仍超需量，保留模型缺口而非虚构电量。
4. 绑定新版基线策略及调度时段，独立重放后才能比较；旧固定功率结果失效，新优化继续固定为模拟基线期末电量。
5. 页面同步显示模拟口径和到限时间，仅加载本地查看，不主动启动新求解或测试套件，不部署。

本地读取结果：基线5305.689067元，充电8h/800kWh，放电7.795217h/725.71925kWh，待机8.204783h，17:47:42到SOC下限2%。当前配置100kW充电至08:00只到75.17%，06:00充满仅为用户举例，不写死。

模拟采用净负荷超需量时优先削峰（可覆盖配置充电/待机），然后受设备能力和SOC限制；无削峰需要按时段请求，充电受需量余量限制。结果是按已确认原则的预测模型，不声称已校验现场EMS固件的迟滞/延时或执行遥测。耗尽后预测有8点需量缺口，保留并披露；新优化仍要求满足需量。

页面时间轴和当前策略已精确拆分到限时刻，15分钟曲线按有效时长折算平均功率。旧固定功率日比较返回失效，旧证据保留。接口只读、DOM/截图及语法/同步检查已完成，无新求解、测试套件或部署。当前8845服务PID72550/会话22800已加载最新代码。

---

# M4 自然日 EMS 基线实施计划

状态：本地实现已接通；用户明确 EMS 原计划已经考虑需量限充、削峰和 SOC 到限规则。此前要求另行提供 EMS 控制 Flow 的阻塞判断撤销，以本文为准。未打包或部署，未运行测试套件。

## 已确认口径

- 周期为北京时间 00:00–24:00，共96个15分钟计算区间；整套采用，不逐段拼接。
- EMS 原计划直接来自本站已确认的时段与功率配置。M4 不再添加另一套限充、削峰或 SOC 截断控制。
- 两方案使用同一全天负荷、光伏、电价、配置效率与零点站级 SOC；优化期末电量固定为原 EMS 日计划的期末电量。
- 新优化方案须满足需量与安全约束，全天费用严格更低才推荐；否则保持原 EMS 计划。全程仅供比较和建议，不向设备下发。
- 用当前预测回算 EMS 原计划购电，不代表 EMS 实测需量；不得据此断言现场 EMS 超限。预测费用差不是实际账单节省。
- 用户暂停测试的要求继续有效；本地取数和页面查看已经授权。没有修改生产配置、构建镜像或部署。

## 已实现

- [x] `daily_inputs.py` 获取自然日连续96点负荷、光伏、电价、EMS时段及准确零点全站 SOC，逐项显示准备状态。保留旧滚动输入语义，不补点、不拿当前 SOC 代替零点值。
- [x] `daily_baseline.py` 展开用户确认的 EMS 原计划，回算购电、SOC、费用与期末电量；对实际参数不一致返回具体原因，不虚构控制行为。确认依据 `user_confirmed_ems_plan` 与时段版本、完整请求摘要和零点采样绑定。
- [x] 优化器模型及独立校验器支持可选 `terminal_soc_target_pct`，日比较固定为基线期末值；旧调用保持原终点范围，未修改旧请求契约。
- [x] `daily_comparison.py` 独立复算安全、需量、期末电量和总费用，只推荐完整且严格更便宜的日方案。EMS 基线不被新预测的需量回算结果否定；新方案仍受需量硬约束。
- [x] `daily_plans.py` 提供按站互斥的本地后台比较任务，读取后再次核对配置、EMS版本与日期；保存最新结果及逐次 JSON 证据。当前逐次证据尚未接入旧“运行记录”分页入口。
- [x] API 新增 GET `daily-inputs`、GET/POST `daily-plan`。页面先显示已计算的 EMS 基线，点击“生成全天优化计划”才启动比较；未采用候选在详情明确标注。
- [x] 线上 HTML、Node-RED HTML 及 Flow 模板同步；网关允许新接口，保留认证、内部网络目标与5个分组。

## 数据口径与实际检查

- 2026-09-09 电站1全天负荷、光伏参考、电价均96/96；零点唯一站级 SOC 为2.375%。EMS 为00–08充100kW、08–12/14–18/19–21放60kW，其余待机。
- 首次基线回算用旧本地效率1.0/1.0，日充800kWh、日放600kWh，期末 SOC 21.532088%。用户侧随后将本地配置更新为v5：1044kWh、充200/放120kW、效率0.95/0.95；助手未写参数，后续以实际请求版本为准。
- 预测由已保存手动任务和滚动结果组成，部分批次在当日中午之后产生。因此标记为同输入日费用回算，不能称为零点已知的日前计划。
- 已完成 Python AST、可执行 JavaScript 语法、JSON/页面同步和实际本地页面读取。没有运行测试套件，也不把这些检查报告为算法回归通过。

## 待后续验证

恢复测试后覆盖：缺点/跨日拒绝；全天等价或更贵回退；更便宜但超需量拒绝；期末少电不可计为收益；配置或 EMS 版本变化；后台失败、重启后的证据复验；新日任务在运行记录中的展示。

## 本地已完成一轮比较（15:42）

- 只读发现用户侧已有后台任务运行，未主动触发新任务。任务 `6d4a2b59-e7a7-4ef5-8d45-46f9970527e6` 完成：基线5480.370733元、优化5207.119847元、预计费用差273.250886元；最大购电504kW，期末SOC均14.675867110304527%。
- 请求使用本地v5配置及0.95/0.95效率，三候选状态返回optimal，比较器推荐optimized。已查看实际页面和结果，浏览器错误为空。此证据仅代表该轮，不是回归测试或生产验证。
- 最新逐次归档代码在当前8845服务启动后写入，待无任务时重启加载；尚未接入旧运行记录分页。

## 2026-09-09 调度参数归属调整

用户授权成本/数据有效期转服务端配置、购电边界直接来自t_need.need_kw；reserved_kw为免调功率，不扣需量值。按用户要求直接实现、不运行测试。

1. 新增服务端配置文件，继承当前两站成本0/有效期86400秒；读配置形成带内容版本的有效快照，保存时忽略浏览器对这两项及旧手填购电上限的覆盖，不自动改写SQLite。
2. 新实时输入及全天基线的购电硬限值均取need_kw；旧历史请求保留原解释，新策略版本区分并废止旧日比较。
3. 表单仅保留效率2项/SOC安全2项/推荐2项；保留滚动历史期末参数但不在当前全天页面填写。同步HTML/Flow，补充来源和配置维护文档。

## 2026-09-09 光伏余电去向开关（用户已确认）

按电站保存pv_export_priority布尔开关，不加入电价、流向图或第二个设置。开启：负荷优先后的余电优先上网，上网受限部分再入储；关闭：余电优先入储，充满/功率到限后再上网。不允许电池向电网放电，不依赖售电收益。

实施：扩展版本化请求的光伏策略；对两种新策略分别在MILP施加余电优先约束并独立校验，保持需量硬限与SOC约束；EMS模拟采用同一余电规则后再做全天同末SOC费用比较。旧保存参数缺该字段时读取默认关闭，新配置版本区分；历史证据仍按旧策略解释。同步HTML开关、Flow、说明。按用户要求不运行测试、不打包、不启动新求解、不下发。
