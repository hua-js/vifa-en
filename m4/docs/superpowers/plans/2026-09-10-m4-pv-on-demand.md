# M4 按需光伏更新实施计划

目标：滚动策略显式启动时复用有效预测，缺失/过期/覆盖不足时触发一次PV任务，等待同一job完成，重新读取并验证同一run_id后计算。GET输入只读；自然日比较不变；失败不自动重发POST，不下发设备。

- [x] 类型化可更新原因；Unix Socket受限客户端及串行更新协调器，600秒有界等待、任务/批次身份核验。
- [x] 候选后台任务接口与页面轮询；内部滚动决策链共享按需准备入口，普通GET不触发更新。
- [x] 本地误差追踪工具：成熟批次与实测匹配、覆盖率、MAE/RMSE、完整日才计算日电量误差；不写生产表。
- [x] 同步独立HTML与网关路由，输出M4共享PV Socket的只读挂载说明。遵循不运行测试要求，仅静态检查；不部署生产、不实际触发预测。

静态检查完成；未运行测试、API写入或求解。误差工具为按需执行，未启用自动采集定时任务。本轮未发布镜像。
