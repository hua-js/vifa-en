# M4 自动生成计划

2026-09-22：本地新增尖／峰段放电600 kW、平段放电540 kW规则，充电保持600 kW；本次未运行测试或部署。

当前本地实现（2026-09-22）：自动任务每15分钟检查日计划，停止自动滚动求解和滚动改表。首次可下发日计划仅处理下一刻钟至当日日末，原求解96点及预测费用/SOC保留。计划表确认后持久化冻结，重启与页面刷新不重复写入；“立即重算”只生成候选，通过“查看新计划”后显式“替换剩余计划”才改未来时段。

电站2充放电建议严格大于10 kW才生成动作，写表设定为充电600 kW，尖／峰段放电600 kW、平段放电540 kW（谷段放电沿用600 kW），实际功率由EMS控制。新记录与被替换/承接的旧记录使用 `repeat=今日有效`；跨日检查将往日M4自有记录标记为“已过期”并保留供观察，没有非空计划 ID（`m4_run_id`）的记录保持原样，不转换有效期、不补写M4身份；不确定写入继续保持hold，不自动解锁或重放。控制参数变化触发首次下发前重算，已确认日计划保持冻结。

充放计划删除仅允许带非空 `m4_run_id` 的记录，远端删除条件同时核对计划 ID、记录 ID 与更新时间；未标记记录需要删除才能完成替换时，保留原表并暂停写入。新建、替换和承接记录写入 `explain`，说明充放用途、分时功率设定、EMS控制实际功率及今日有效；说明字段纳入回读确认。正式页面移除滚动计划历史展示，历史证据仍保留。

凌晨保底独立于日计划求解：仅在日计划未就绪且处于原谷电窗口时生成一次充电/待机安排，日计划接管后不再自动保底改表。实时输入过期/安全检查失败仍阻断；不因该通道启动滚动求解。

往日历史处理：用户已确认 EMS 支持 `repeat=已过期`。跨日检查仅将往日带非空 `m4_run_id` 和有效 `m4_plan_date` 的记录更新为“已过期”，更新条件同时绑定计划 ID、记录 ID 与更新时间，保留原日期、时段、功率和计划 ID，不删除；已过期记录不参与当天读取及替换匹配。当天冲突记录的替换／删除保持原规则，不增加配置开关。已补充回归用例，未运行测试、未部署或生产写入；本轮未独立验证 EMS 执行。

`M4_AUTO_PLAN_ENABLED`控制自动检查；生产电站2写表仍须保留三份Compose配置与`M4_EMS_STATION2_TABLE_WRITES=1`。页面按保存的计划版本展示预测，实时数据失败不清空已确认日计划；明细按历次确认版本生效边界保留已过时段。新增 `POST /daily-dispatch` 接受 `request_id`，Node-RED代理白名单及导入Flow中对应函数同步更新。

2026-09-22用户反馈测试失败后，补齐共享夹具的日末/滚动参照版本，分离历史保电模型与当前准入夹具，同步“今日有效”报文断言，并补全模拟请求和电站身份。未放宽生产安全校验。M4离线660项（167.414秒）、前端/网关37项及日计划历史展示脚本通过，日志见`outputs/m4/tests/2026-09-22-daily-schedule-repair/`。未进行实际页面渲染、业务重算、构建或部署。`今日有效`是否被EMS按预期到期执行、真实设备功率与保护均未联调；写表回读不代表设备执行。下文旧滚动段落为历史实现说明，不再描述自动生产入口。

## 当前规则：尖、峰段只允许光伏余电充电（2026-09-21）

尖段 `jian`、峰段 `feng` 禁止电网充电；允许光伏满足负荷后的余电充电，仍遵守设备功率、SOC、光伏分配策略和可选 EMS 方向限制。平段、谷段沿用原规则。独立候选校验、日计划最终推荐、滚动回退及写表预览均检查该规则。

原 EMS 计划按原控制逻辑回放，保留为费用与期末电量比较基线。如果包含尖、峰段电网充电，可以参与比较，但不能作为回退推荐；没有通过既有门禁的合规候选时，日计划显示阻断、滚动建议暂停，不能自动沿用违规基线。此变更不放宽安全 SOC、期末电量或全天100元收益门槛；过程保电采用下述 v4 软目标。

新请求绑定 `source_versions.grid_charging_policy=peak-grid-charging-v1`，求解器版本同步带该标识。缺少或不匹配该版本的日计划、滚动结果不能作为当前方案复用，写表预览也拒绝旧版本。当前方案及写表安全检查要求完整电价时段标签，不能因标签缺失绕过禁充。历史证据保留，此次未触发重算或生产写入。

站2严格大于10 kW及写表报文分时功率 的联调口径保持不变。本规则基于预测与建议功率进行校验，不代表设备实际功率闭环或实时光伏余电保证。

禁充变更阶段（过程保电软化之前）相关离线专项163项通过，覆盖尖／峰／平／谷与光伏策略32组求解组合、独立校验、日／滚动回退、旧版本失效、标签缺失拒绝、凌晨谷电及写表预览。日志：`outputs/m4/tests/2026-09-21-peak-grid-charging/unittest.log`。

## 当前规则：电站2过程保留电量为软目标（2026-09-21）

日计划采用 `m4-daily-operating-floor-v1` / `peak-reserve-v4`，滚动请求沿用同一保电规则。从最后一个连续尖／峰段的起点至日末，允许过程保留电量出现缺口；没有未来尖／峰段的滚动尾段从当前起点计算。求解器在原保电优化层内最小化峰前准备缺口与逐状态保留电量缺口之和，不改变其之前的需量、费用及其他既有目标顺序。

安全 SOC、设备及电网功率边界、尖／峰段禁止电网充电均保持硬约束；电站2新日计划日末 SOC 下限为安全下限与推荐下限的较高值，不再跟随 EMS 模拟期末余量；滚动继承已确认日计划的同一目标。不能仅因起点或下一刻钟低于保电目标而判无解，但期末电量不可达时仍无解，不自动放宽或回退到违规方案。存在过程缺口的可行候选带 `PEAK_RESERVE_SHORTFALL` 提示；它不等同于保电优化层未完成，也不单独阻断候选。

v1–v3 保留各自历史建模与校验含义，旧日策略不能作为当前结果复用。正式 HTML 仅新增 v4 版本映射，不同步 Flow 或副本。

本轮后端相关专项107项通过；随后补充并复验核心9项，覆盖低起点、下一刻钟缺口、光伏补能、无未来峰尾段、暂用保留电量、期末不足与安全下限。页面脚本13项通过，独立代码审查未发现有证据的P1/P2问题。日志位于 `outputs/m4/tests/2026-09-21-soft-peak-reserve/`（`unittest.log`、`focused.log`、`frontend.log`）。未运行全量或实际页面渲染验收，未部署、生产重算或写表。

## 历史实现：凌晨谷电直接充电（2026-09-21）

电站2凌晨在零点连续谷段与固定 EMS 充电窗口的交集内，直接生成充电／待机滚动安排，不再等待 M3 预测、预测质量评分或日计划。白天仍走原日计划与预测质量门禁。当前和下一刻钟均须在该窗口内；不补发过去时段，例如00:01生成00:15起的计划。

- 使用全柜实时SOC、功率、状态及本站实时负荷／电网功率，按当前配置容量、SOC上限、充电功率及需量限制生成计划。采用当前负荷保持不变的保守估计，不计未来光伏收益；不保证一定充满。
- 任一柜不可用、实测缺失或过期、安全边界不满足时阻断。读取配置后及提交写表前复核柜和站负荷数据时效；到达SOC目标后待机。
- 沿用现有写表与回读链路，写入／替换的记录携带 `m4_run_id` 和 `m4_plan_date`；数据库数值 `id` 仍只用于定位记录。窗口外原EMS记录不追加M4标记，不删除白天安排。跨越窗口末端的已有记录继续阻断替换。
- 保留hold、版本与回读校验，以及建议功率严格大于10 kW、电站2写表报文分时功率的联调规则。回读确认只表示计划表确认，不代表设备实际执行。
- 无日计划时前端可展示凌晨充电安排，下发结果仍看调度记录；凌晨安排不提供全天节省金额。

当前工作区离线专项75项通过，覆盖夜间独立入口、SOC边界、数据时效、白天门禁、窗口外记录保留、M4标记和模拟回读。日志：`outputs/m4/tests/2026-09-21-direct-night/unit-tests.log`。未运行全量或UI渲染检查，未启动服务、部署或触发生产写入；真实写表和设备执行未验证。

此前 `f9dffc4` 的质量例外方案仍要求完整预测，其67项测试和06:15–08:00保存数据回放属于旧方案证据，见 `outputs/m4/tests/2026-09-21-startup-admission/验证结果.md`，不代表当前凌晨独立路径的现场执行。

## 2026-09-12历史验证结果（不覆盖滚动版本）

自动任务及接口相关6项通过，网关19项通过。启动本地服务后两站生成新run_id且completed，无需浏览器POST；页面显示自动状态并读取优化方案，未出现脚本错误。记录明暗1440/834/390截图并检查桌面/手机渲染。当前未部署线上；本轮未重跑全量套件。

保持单个应用进程提供计算任务服务；多进程的自动调度通过leader锁去重，但现有手动任务状态仍在各进程内存，不在本次扩展为跨进程作业队列。服务重启时同一15分钟窗口已发起的任务不重复启动，未完成或失败任务在下一窗口重新生成。

## 历史实现：剩余日滚动建议（2026-09-14实施）

目标：保留每天首次成功生成的全天计划及单站全天100元净收益门禁，每15分钟读取M3实际负荷和设备SOC/功率，生成下一刻钟至日末的独立建议；未下发。

实施步骤：
- 扩展Optimizer窗口契约：仅带`remaining-day-v1`标识的请求允许1–96点，必须止于北京时间日末；旧请求仍限制95/96点。
- EMS模拟按每个点的北京时间匹配原配置，支持显式剩余日窗口。两套方案从同一最新SOC投影状态起算并保留安全、需量及期末电量校验。
- 新增滚动服务，保存请求、基线、候选、实测来源、日门禁引用与建议。使用最新M3预测计算未来，实际负荷独立保存用于运行对照，不拼接为未来预测。短时SOC投影采用实测功率，过期/越界阻断。
- 自动入口每日首次成功生成日计划，每刻钟生成滚动建议；当日门禁不重复按剩余100元判断，但剩余优化必须比同起点EMS净费用更低。
- 前端独立展示滚动时段建议及预计SOC，保留原全天比较，避免把已过去的计划改写成实测记录。

本轮仅源码实施与静态审阅，不启动服务、触发任务、运行测试或部署；验收待授权后覆盖时间边界、SOC时效、两站收益门禁、失败恢复和接口展示。

### 无备用要求下的日末目标（本地实现，尚未验证部署）

电站2使用`daily-operating-floor-v1`：没有停电备用电量要求，日末最低SOC来自配置的安全/推荐下限，不要求强制放到该值。日计划、滚动和写表校验使用同一目标；滚动不再按最新EMS余量抬高日末目标。电站1、光伏余电策略及凌晨独立直充通道保持现状。

EMS原计划仍是比较基线。优化方案比EMS少留的日末电量按本日谷段最高单价与充电效率估算补回成本，从费用节省及净节省中扣除；多留电量不额外奖励。估值随日计划固定，滚动继承，即使剩余窗口无谷段也不重定价。该估值不是次日价格预测或可充入保证。全天100元门槛及滚动净改善门槛基于调整后收益。

新日计划策略版本使旧当前计划失效；新写表入口拒绝旧日末策略，历史归档保持原样。本次已完成源码审查、Python语法解析及差异检查，并按用户要求用9月13日/19日实测作本地日计划及白天滚动回算：两天净节省117.37/145.11元，均通过100元门槛；128个滚动起点中120版通过安全检查，8版因实测SOC超上限被阻断。未运行完整测试套件、部署或写表。历史预测准入假设通过，未还原凌晨直充及设备闭环；详见`outputs/m4/evaluations/2026-09-21-operating-floor-recalc/回算结果.md`。

### 日末下限处的滚动待机回退

新滚动参照`remaining-ems-operating-floor-v1`在副本中将EMS放电下限提高到当天运行下限，用于滚动收益比较和保守回退；不修改原EMS配置、日计划比较基线或历史记录。已到运行下限（数值容差内）时，不采用先放低再补回的候选，写表安全入口独立复核。初始SOC高于下限时原过程软目标保持。

无更经济合规候选时，若参照方案全程待机且当前已到下限，正常返回“已到最低运行电量，待机等待谷段。”；若待机仍有需量、安全或日末缺口，继续阻断，不能以成功状态掩盖不可行。光伏策略、尖峰禁电网充电及凌晨直充不变。旧滚动参照版本不再作为当前建议或新写表输入，历史归档保留。

本地17项针对性测试通过；以9月13日18:00剩余实测曲线、起点SOC设为2%的场景重新求解，两个先放低后补回候选被候选/写表边界拦截，选择全程待机且日末2%的回退，原始及下发功率归一化后安全检查通过。属于本地场景验证，不是生产当前预测或完整服务写表闭环。证据：`outputs/m4/tests/2026-09-21-operating-floor-fallback/`。未部署。

### 最低运行电量附近的待机容差

用户确认0.05个百分点待机容差。`remaining-ems-operating-floor-v2`仅对起点处于运行下限以下0.05个百分点以内（含边界）、且不低于安全下限的零功率全程待机方案，允许保留现有电量缺口，不要求补电；不降低求解器日末目标，不增加可放电量，不放宽需量、电网或安全SOC。接近下限时不允许先放低再补回。安全待机参照满足容差时不再为补齐该微小缺口选择充电候选；未来有正常充电安排的非全程待机方案仍按原目标校验。

本地20项专项通过：1.975%、1.95%待机可用，1.949%阻断，伪造预期SOC的微小放电也在重算校验中阻断。使用用户SOC=1.975%、功率=-2.75 kW及时间戳推算18:15为1.987190%；配合既有历史负荷/PV曲线，生成全程待机并通过原始/下发功率安全检查。该回放假设预测质量准入通过，不是线上当前预测闭环。证据：`outputs/m4/tests/2026-09-21-operating-floor-fallback/idle-tolerance.log`及`idle-tolerance-replay.json`；未部署。

## 日计划独立下发调整（2026-09-22，本地开发）

已确认：停用自动滚动求解与滚动改表，保留日计划生成和失败重试。求解建议不改写为600 kW；建议功率严格大于10 kW才形成充放电安排，EMS表设定为充电600 kW，尖／峰段放电600 kW、平段放电540 kW（谷段放电沿用600 kW），实际功率由EMS控制。预测SOC/费用不代表实测执行。

实施范围：
- [x] 独立日计划下发与持久化版本控制；重算不自动覆盖已确认日计划，显式替换仅影响未来时段。
- [x] 独立凌晨保底：日计划未就绪时每个窗口最多确认一次，日计划接管后停止；保留实时输入与安全检查。
- [x] 新写入统一“今日有效”；检查旧M4重复记录，往日记录默认保留，保留不确定写入hold。EMS端对“今日有效”的实际到期行为尚未联调，不宣称设备闭环。
- [x] 页面只用日计划预测曲线，分别显示建议功率、分时功率设定、写表状态和实测；旧滚动历史保留。
- [x] 补充专项回归用例并代码审查；用户反馈报错后已运行相关及完整M4离线测试，结果见本文开头；未部署、不写生产接口。
