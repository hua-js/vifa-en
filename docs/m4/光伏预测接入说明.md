# M4 只读光伏预测输入

M3负责定时或管理员手动生成光伏预测，M4通过现有NocoBase只读接口查询 `energy_pv_forecast_runs` 与 `energy_pv_forecast_points`，复用M3发布完成的批次。M4不调用生成接口，不更新天气，不训练模型，不轮询预测任务。使用原有平台凭据，两表只需list权限；不需要PV控制Socket。

## 当天短时偏差校正（2026-09-17，本地未部署）

用户确认实施顺序：先使用多个已完成日期按当时可用数据逐时回放，确认短时误差和光伏高估风险改善，再接入滚动求解。原M3预测、原始日计划及已归档滚动建议保持不变。

- [x] 校正器与专项用例：最近四个已结束15分钟区间，每段至少12个有效分钟，分钟内均值后再等权聚合。至少3段有效，且最近闭合区间必须有效；至少3段同方向超过5%才修正。原预测低于20kW不参与比例拟合也不放大；比例中位数限于±30%。未来一小时完整修正，第二小时线性衰减，2小时后回归原预测。
- [x] 只读获取已完成批次及实测并回放。每轮选择当时可用的最新批次（以已验证批次的forecast_start保守代替可用时间），切换后只用新批次覆盖的闭合历史标签，目标从下一刻钟开始；同一未来时段可能在多轮出现，评估是滚动建议实例而非独立样本数。历史实测缺少到库时间，无法证明当时采集延迟，属于事件时间回放，不是现场在线验收。
- [x] 接入滚动输入：双向校正保存为诊断估计，求解输入使用`min(原始预测,校正预测)`，不因上调光伏释放额外充电空间。下调为净负荷预测增加对应余量；不是经校准的置信下界，不覆盖全部光伏/负荷误差。EMS基线与优化器采用相同保守输入及同起点SOC，重新计算期末电量约束。无实测/读取失败回退原预测并留原因，来源不一致或值异常阻断。
- [x] 每轮存证`pv_correction`（观测、原值、修正值、求解值、源批次、哈希及策略），独立源版本`pv_correction`参与请求；滚动版本升级`remaining-day-pv-correction-v2`，历史v1契约仍可读取。
- [ ] 镜像构建、部署和线上滚动闭环验收；没有新增设备下发或实时并网点限充控制。

回放固定初始参数、未做参数搜索。`outputs/m4/pv-correction/20260917/replay.json`涵盖9月10–16日、7个有白天样本的日期、2212个滚动目标对：原始MAE74.16kW；双向校正60.70kW，但3天高估增加，因此未直接作为求解输入。保守求解输入MAE68.41kW，平均正向误差从36.14降至26.82kW，各日高估均未增加；9月10日MAE由69.88增至73.85kW，不声称每天或所有天气均改善。验收门槛为至少5个有效日期、总体MAE下降、各日平均正向误差不增加。此为同一批历史数据上的初步验证，不等于持续训练更新后版本或独立留出集效果。

验证：校正/遥测读取/批次切换8项、滚动输入及升级回退6项、优化契约/光伏策略/独立校验/只读来源34项，共48项相关用例通过；沿用本轮有效结果，仅重跑受修改影响的用例。独立审查发现的批次切换回放偏差已修复并复核。`git diff --check`通过。未执行全量测试、真实线上求解、镜像构建或部署。

长期回放入口：`.venv/bin/python m4/scripts/replay_pv_correction.py --reports <pv_accuracy报告目录> --output <报告文件>`。未来训练源更新后应再评估此校正器。安全执行仍需EMS根据实时并网点功率限充，不能把预测修正当作防超需量保证。

## 高估退化后的余量候选（2026-09-17，影子记录）

本轮比较固定比例、旧新模型取低以及近期正误差余量等候选，属于使用同一7日数据的方案探索，不能称为独立留出验证。固定当前候选规则后，再读取今天未参与前轮评估的09:45、10:00两个完整目标区间。

候选规则：保持中央偏差估计不变，先扣除已经应用的向下偏差校正，再取最近1小时有效样本的最大正误差作为额外购电余量；每个目标最多为原光伏预测的30%，沿用1小时完整、2小时归零的衰减。它是经验性保护余量，不是置信区间，不包含负荷预测误差，也不代替实时限充。

- 更新模型：原保守校正MAE64.61kW/平均正误差29.31kW；加入余量候选后为62.77/19.24kW。
- 旧模型同样加入余量为66.31/17.67kW，故仍不能声称更新训练相对同保护旧模型全部指标无退化。
- 今天2个新增目标、16个重叠滚动对：高估均为0，加入余量后的MAE38.11→43.29kW。这是极小的未参与选择目标检查，缺少真实历史到库时间，不构成多日独立验证。
- **决策：新增余量仅影子记录，不用于滚动求解。** `read_correction`显式关闭`apply_recent_reserve`，`solver_kw`仍为原预测与中央校正预测的较低者；候选在`reserve_candidate_kw/reserve_kw`留档，`reserve_applied=false`。离线回放可启用候选做比较。
- 校正记录版本`pv-recent-bias-v2`，滚动策略`remaining-day-pv-correction-v3`，旧v1/v2契约仍兼容；策略升级后的旧当前结果显示待更新，历史记录保留。
- 本轮29项校正/滚动/契约专项通过，独立审查完成。未部署、重算线上计划或下发，后续需更多独立日期再决定是否启用余量。

证据：`outputs/m3/validation/20260917-continuous-training/replay-reserve-v2/decision.json`、`holdout-20260917/report.json`。候选实现不解除此前联合版本暂缓发布的结论。

## 批次与时间校验

- 只接受ES02、operational、completed的最新批次（as_of/id倒序），生成时间不得晚于查询时间。
- 单批15分钟连续96点，校验关联、时间戳、步数及有限非负功率；来源版本绑定批次、内容哈希、天气与模型信息和读取值。全天零填补另保存策略、原覆盖点数、填补点数与时间列表，并加入source_versions供记录追溯；页面图表提示按零估算。
- 全天比较按当天00:00–24:00对齐。2026-09-16确认：仅北京时间20:00–次日07:00允许窗口外缺测补零，已有预测值保留；07:00–20:00任一缺测阻断日计划，等待M3完整输入。不读取历史实测代替预测、不拼接批次。整天无重叠、整批缺失、批内缺点、损坏和读取失败继续阻断。
- 只允许今天或昨天生成的批次。日输入记录完整性版本 `pv-night-zero-20-07-v1`，站2旧版本日计划退出当前方案但保留历史；页面缓存同步失效。M3通常06:00生成06:15起的24小时预测，当天00:00–06:15的缺口可按已确认范围补零；07:00–20:00仍须完整覆盖。
- 电站1的零光伏来源保留。缺失、过期或覆盖不足时阻断新方案并提示等待M3更新；历史调度记录保留，不当作有效的新计划。

## 本次代码与部署边界

`daily_inputs.py`接入已发布光伏读取器，`api.py`与`candidates.py`移除按需预测入口；`pv_on_demand.py`仅保留M3读取共用的SocketConnection传输类。旧主动预测测试替换为只读与窗口校验测试。浏览器快照版本更新，避免新版页面恢复旧口径快照。

需构建并部署新M4镜像后生效；现有M3镜像不变。PV输入已改为只读查询，不再需要PV控制Socket；原先承载该配置的 `m4-pv.override.yaml` 已移除，其网络等内容并入 `m4/deploy/backend/m4-production.override.yaml`，生产侧也须把旧的PV Socket挂载清理掉。

以下9月10日记录属于旧补零规则，不代表当前规则验收。

本机只读联调成功：9月10日09:46生成的批次（北京时间）仍可用于当日回算，实际预测56点、窗口外补零40点；未触发预测或设备指令。生产部署与真实完整窗口方案尚未验证。

## 2026-09-10 线上只读联调验收

- M3查询与M4 daily-inputs均返回200，run_id一致：`a7995e69-254f-48cd-aae4-8497e5af6cf5`。逐时间戳比对56个预测点全部一致，40个窗口外时段均为0；M4 can_compare=true、missing为空。MAPE为7.1821%，只读请求未生成预测或求解。
- daily-plan仍保存旧光伏参考版本`pv-reference-b2b20a8b6444672e0ec8`，与新输入来源不一致。新版HTML增加当前页PV版本一致性校验，不一致时展示新输入EMS基线；历史记录保留，等待用户重新生成方案。缓存版本升v3，避免旧视图继续恢复。
- 线上HTML未验收：认证头访问/m4返回401；URL带凭据的检查被自动审批拒绝，未执行。需在Node-RED的现有M4 HTML模板节点同步仓库 `m4/web/M4优化调度控制台-线上版.html`；不需要替换整套Flow，也不应仅因后端镜像已更新就认定页面已更新。
- 明早06:00需在服务器核对 `/run/vifa-pv/manual/daily-schedule.json` 的当日job_id，并查看对应 `jobs/<job_id>/job.json` 为completed；仅有提交记录不足以证明预测完成。随后确认M3最新批次与M4来源一致。当前未观察到下一次定时执行。
