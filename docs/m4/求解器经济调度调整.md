# 电站2经济调度 v3

2026-09-12，用户要求先修改求解器的经济性，不关注下发功率。此变更不接入设备。最终确认恢复自主经济调度，不以原EMS时段为新计划的硬方向边界；中间边界试验仅保留历史重放。电站1后续经济优先口径见文末，不能与电站2目标顺序混用。

## 约束与实施

1. 引入peak-reserve-v3，v1/v2历史语义保持不变；当前电站2切换v3，旧当前结果失效。
2. v3取消非峰段仅可需量放电的硬边界，保留净负荷上限、SOC、功率、需量、光伏分配及晚峰起至日末的储能下限。
3. 首峰储能准备降至费用目标之后，避免为充满而牺牲经济性；保留需量优先与候选费用比较。
4. 同步独立校验、页面版本识别与说明；不修改下发实现。

## 验证

先以合成晚间平段负荷回归证明v2禁放、v3应经济放电，再对本地已保存电站2输入重放，比较费用、晚间电量、日末SOC与独立校验。原始输入与v2记录保留，结果写outputs/m4。

## 同输入重放结果

电站2使用本地已保存2026-09-12输入，原EMS预计费用2116.41794元，v2候选2228.16947元（未采用），v3候选1933.90693元（采用），相对EMS节省182.51101元。19:00–21:00窗口放电446.82130kWh，约20:30结束放电，日末SOC为2%。此为预测模拟，不代表实测收益。

模型、契约、独立校验、目标顺序和历史v1/v2等52项相关测试通过；后补v3拒绝错误目标顺序测试后经济调度4项通过。本地8847旧版页面已实际渲染新版方案及182.51元节省；未部署或下发。

全量scripts/check.py m4执行464项，14失败23错误；与修改前已保存基线的失败/错误条目完全一致，无新增条目。完整日志保存在同一验证目录。

## 原EMS时段边界试验（已撤销，不用于当前求解）

用户纠正后，为所有新日计划加入按原配置展开的96点方向边界：充电时段仅充电/待机、放电时段仅放电/待机、未配置时段仅待机。字段缺省的历史请求保留原义。独立校验检查方向，并在日比较时校验方向来源匹配原EMS配置；原EMS模拟保护动作不受新增求解边界约束。新当前结果要求original-directions-v1来源版本，旧当前记录失效。光伏剩余电量分配将禁止充电时段的可充功率视为0，避免与时段硬边界冲突。

同一电站2输入：18–19点全部待机，19–21点恢复放电；新方案费用1994.19378元，相对EMS节省122.22416元，日末SOC2%。边界降低了可选空间，因此收益低于无时段限制的182.51元。下发逻辑未改。

边界回归：62项相关测试通过，追加独立校验拒绝越界和待机光伏余量分配后边界5项通过。已在旧版页面验证18:00为待机0kW、完整时间条18–19待机/19–21放电，并显示预计节省122.22元。

边界修改后全量468项：14失败23错误，失败/错误条目与前轮完全一致，无新增条目。

## 当前最终口径

用户再次更正：恢复求解器自主经济调度，允许18–19点放电，不以原EMS时段为硬边界。新日请求不再带时段方向限制，带该限制的调试记录不再作为当前结果；可选字段仅保留历史重放兼容。晚间放电结束时间仍由经济性和SOC约束决定，不强制固定到21:00。原同输入无时段限制方案约20:30停止放电。

## 电站1需量与物理上限分离

用户确认504kW为希望控制的需量目标，物理购电上限最终更正为550kW（替代先前580）。电站1服务参数明确550kW；全天与实时请求分别传入需量504及物理550，保留需量优先目标。日比较允许有不可避免的需量偏差，但候选不得恶化基线需量峰值及同峰值下的超限电量，仍需费用下降才推荐。页面按550硬上限校验，说明需量目标偏差。电站2尚未确认独立物理上限，保持原口径。

同输入回放：电站1三个候选均optimal，费用从5050.21802元降至5017.58381元，节省32.63421元；最大购电521.444844kW，目标超出17.444844kW，超限电量11.1622175kWh。物理上限未突破。

物理边界验证：13项相关回归通过，追加不可突破物理上限用例后专项3项通过；全量477项14失败23错误，与此前失败条目完全一致。浏览器已验证电站1优化方案、504/550口径和32.63元预计节省。


## 电站1经济优先（2026-09-12补充确认）

504 kW为用户自设控制目标，不是需量计费门槛。550 kW为物理硬上限，SOC、充放电效率、设备功率及日末电量约束保留；不能因经济目标放宽。

实施范围为电站1全天比较：三个候选均先最小化购电费用，再按需量峰值偏差、超限电量择优，最后保留SOC偏好和吞吐量等择优层。当前循环成本配置为0，不虚构需量计费成本。电站2及实时候选顺序不变。

新请求economic_policy=station-1-cost-first-v1，合约限制电站和550上限；日计划检查策略标记及完整目标配置，旧方案不再作为当前方案。新策略取消相对EMS的需量偏差不恶化筛选，但保留独立硬约束校验、相同期末电量及费用降低要求。

验证先用有限电量的平/峰段用例复现旧排序提前放电，再确认新排序留给峰段；同时校验错误电站和600上限不能冒用新策略，回归550硬约束和电站2经济保电。13项专项通过。本地真实输入重算费用5013.25元，EMS5049.39元，节省36.14元，最大购电524.64 kW；08:00放电0.91 kW，后续部分平段仍因高负荷需要放电。仅本地回算，不代表现场执行结果。


## 电站2同成本连续性择优（2026-09-13）

用户确认：避免22:45孤立15分钟放电；不禁止晚间放电，不牺牲费用或需量、SOC、日末保电约束。实施采用站2日计划的末级择优：先锁定原有需求、经济、保电、SOC及throughput目标，再最小化相邻站级净储能功率的绝对变化总量，包含计划两端相对0功率的变化；最后保留原谷充时序偏好。新profile版本带station-2-continuity-v1，旧计划按既有profile匹配规则失效。

该规则是减少功率跳变的偏好，不等同精确最少启停次数，也不增加最小放电功率或固定禁放时段。使用连续辅助变量，不改变原始可行功率集合；费用仍按原分层锁定容差保护。只作用站2日计划，站1和历史请求保持原规则。当前没有重新求解或下发，不能宣称22:45已被移除；验证待用户授权测试或发布时执行。

本地重算结果（用户2026-09-13明确授权）：run 7a7cc976-82c5-4388-b8bd-17e979bca482采用连续性新版本，晚间放电连续到20:15，此后待机，22:45孤立放电消失。全天费用1193.59元，相对EMS节省46.02元，日末SOC34.7184%高于基线34.4243%。与上一结果费用按分相同，但请求points不完全相同，因此不称同输入严格对照。只验证本次实际求解结果，未运行完整测试或下发设备。

连续性范围扩展（2026-09-13）：用户确认电站1同样适用。站1采用station-1-continuity-v1，仍以购电费用为第一目标，504控制参考、550物理上限及SOC约束保留；power_variation置于throughput之后、谷充时序之前。站1新profile通过既有版本匹配使旧日计划失效；旧历史请求仍可按原合约读取。本次只改源码，未重启、测试或重算电站1。

### 电站1连续性优先于SOC偏好（2026-09-13）

用户确认调整23点再次放电的择优原因。station-1-continuity-v2顺序为：购电费用、需量峰值偏差、需量超限电量、光伏未利用、吞吐量、功率连续性、SOC优选偏离、谷充时序。3%–95%只是偏好，排在连续性之后；配置的1%–98%硬边界、550kW硬上限及日末电量约束不变。保留吞吐量在连续性之前，避免通过额外充放电改善平滑性。旧v1合约仍可读取历史，新日计划按完整profile版本判定失效。电站2排序不变。本次仅改代码，未测试、重启、重算或下发；需后续重算确认23点是否合并，不能保证任何输入下均单段放电。
