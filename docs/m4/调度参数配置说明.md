# M4 调度参数配置

## 当前配置归属（2026-09-09）

本节覆盖下文早期“13项手工参数”等历史说明。页面提供“光伏余电优先上网”开关，以及8项数值参数：单柜最大充电功率、单柜最大放电功率、充电效率、放电效率、SOC安全下限/上限、SOC推荐下限/上限。效率用于电池侧/电网侧电量换算及费用估算，安全SOC用于停止充放，推荐SOC影响候选排序；当前适配器尚未接入这6项的上游配置来源，因此保留人工维护。

- 总容量：`t_es:list.es_power_storage`，按`sn`匹配电站。
- 单柜最大充放电功率：在“调度设置”中填写，允许0；页面按接口柜数折算为整站总功率保存（电站1×2，电站2×6）。`t_model:list`对应模式最大`kw`仍作为接口允许上限，输入不得高于该值；计算同时取人工设置和接口限值的较小值。基线各时段仍按原计划行功率，再受所设最大功率约束。新站默认显示接口上限，已有配置按柜数除回单柜值，不自动覆盖已存设置。保存后需要重新生成计划生效。
- 购电硬上限及需量目标：统一来自`t_need:list.need_kw`。取消原人工购电上限；`reserved_kw`为免调功率，只作来源展示，不从`need_kw`扣减，也不增加超限额度。本次未新增现场免调死区控制算法。
- 初始SOC：实时/自然日零点采样，不手填。电站1全天比较的期末SOC固定等于EMS模拟期末值；电站2按下方峰段保电规则保留日末电量，不强制放到EMS末SOC。原滚动期末允许偏差保留在已存参数中，但当前页面不编辑。

### 光伏余电优先上网

按电站保存`pv_export_priority`。新站及旧配置缺省关闭：优先满足负荷，再将余电入储，电池达到SOC/充电功率上限后剩余余电上网；开启时优先满足负荷，再优先上网，上网受限部分才入储。没有新增电价或上网功率表单，也没有电量去向展示；沿用现有后端外送边界，不将开关解释为外送许可。电池放电不能外送，光伏有余电时不同时购电充电。

两种顺序作为优化硬约束，不由售电收益决定。需量、SOC、设备功率等安全约束不放宽；无法满足时不输出违规方案。EMS模拟基线应用同一个客户余电配置，其余时段沿用原计划及需量/SOC规则；这是配置下的模拟，不代表已变更现场EMS。电站1全天比较仍要求同末SOC；电站2要求优化日末电量不低于EMS，且净电费严格更低才采用，否则展示该配置下的EMS模拟基线并明确未采用保电优化。售电价仍0，本次不计售电收益。

设置进入配置快照与版本；历史证据保留旧光伏策略，新输入采用显式storage/export优先规则。更改后需重新生成日计划；旧日比较版本失效。只保存本地设置，不向EMS下发。

### 电站2全天计划：峰段保电

日策略版本为`m4-daily-peak-reserve-v2`，仅用于电站2从零点SOC起算的全天预览；电站1、滚动候选和已归档请求保持原规则。当前电价接口只提供谷/平/峰，使用真实`feng`标签识别峰段，不硬编码10点或14点、不虚构独立尖峰价格。

- 非峰段禁止套利放电，仅允许满足需量/购电硬上限必需的放电。该功率仍受设备能力、SOC和禁止储能外送约束。
- 三候选在需量两层之后，优先最小化首峰起点相对推荐SOC上限的储能缺口；达到推荐上限后不再奖励增加电量。之后沿用各候选推荐SOC、费用/PV、吞吐和凌晨谷段早充目标。此目标避免取消平段放电后通过少充电抵消备用电量；未确认该层完成的候选不作为保电推荐。
- 新请求显式使用`peak-reserve-v2`。从当天最后一个连续`feng`峰段的起点至日末，每个电量状态均不低于日末SOC要求，晚峰不能先消耗该保留电量再于峰后回补。按实际电价标签定位，不硬编码时刻；无可行解时不放宽底线。旧v1请求继续按原规则回放，不能作为新服务的当前方案。
- 日末SOC至少为`max(EMS模拟末SOC, 推荐SOC下限)`。峰段已到净负荷或功率上限时，剩余电量保留到日末，不强行加大峰段功率，也不为了对齐1%而在23点后放电。
- EMS基线继续按原配置独立模拟，不套用新优化器的非峰限制。新优化仅在日末电量不低于EMS且费用严格更低时采用；费用差不对保留电量另行估价，不称为同末电量比较。
- 新服务拒绝把旧日策略作为当前结果；新规则加载后需重新生成。页面可兼容尚未升级的后端，但明确提示v1旧规则未生效，不能把它称为v2结果。输入准备阶段只有EMS基线，不能据此声称已生成保电计划。

峰前准备和保留电量仍依据当前预测，不保证任意负荷/光伏偏差下都够用；当前全天功能没有按实测SOC滚动重算或真实设备闭环。安全SOC和功率上限未放宽，参数未自动改写。

### 服务端配置文件

维护`m4/settings/runtime_config.py`，修改后重启后端生效；无须在客户页面填写成本或时效。默认值沿用当前两站已有配置：

```python
GLOBAL_RUNTIME_DEFAULTS = {
    'cycle_cost_per_kwh': 0.0,       # 元/kWh，充电量+放电量的吞吐成本系数
    'max_input_age_seconds': 86400, # 秒，沿用既有值，不是生产时效推荐
}
STATION_RUNTIME_OVERRIDES = {}     # 可按station-1/station-2部分覆盖
```

成本须为非负有限数，有效期须为1–86400的整数秒。该时效沿用既有实时柜隔离/请求有效性校验，不是预测或电价自动补齐规则。成本指标与购售电电费分开，不能将其称为实际电费。

读取配置时，服务端将上述两项覆盖到有效参数快照，并移除旧手填购电限值；内容哈希附加到有效配置版本，变化后旧输入和结果不能作为当前配置使用。读取不会自动重写SQLite；保存时忽略客户端对成本、时效、旧购电限值的覆盖。未配置站仍需先填写效率和SOC。历史证据按保存的原参数解释，不回写。

本次未运行测试或浏览器验收、未打包或部署；HTML/Flow模板已同步，后端需加载新代码后与页面配套使用。

更新日期：2026-09-08。范围：两站参数维护、真实输入核对、数学候选计算与显式偏好选择。选择入口见[选择偏好配置说明](选择偏好配置说明.md)；本阶段仅生成预览，不自动调度或下发 EMS。

## 页面使用

从本地服务打开 M4，点击右上角“调度参数”。上方显示本站接口配置，下方填写剩余人工参数；电站 1、2 分别读取和保存。页面草稿按站保留，刷新后丢弃；已保存内容从服务端恢复。

### 接口配置，不再重复填写

| 来源 | 字段与口径 |
|---|---|
| `t_need:list` | `f_es_sn` 归属；`need_kw` 为需量阈值，`reserved_kw` 为免调功率；`rated_capacity` 单位 kVA，不能直接作为购电 kW 硬上限 |
| `re_flow:list` | `fk_es_sn` 归属；`re_kw` 显示为防逆流配置 kW；用户确认目前未启用方向控制，40 kW 无实际作用，不参与优化约束 |
| `t_model:list` | `es_sn` 为电站数组；`start_time/end_time/type/kw/repeat`；用户确认功率是电站总功率、每天重复 |

移除三个手填字段：`demand_limit_kw`、`grid_export_enabled`、`grid_export_limit_kw`。需量采用接口原精度，不把 `reserved_kw` 从 `need_kw` 擅自扣除。免调功率只展示，当前优化器没有模拟 EMS 的免调触发逻辑。

现行每日计划只读展示，不锁定优化时段、不替代最大充放电能力。未列时段标为未配置，不补停机或延续动作，也不声称该配置等于实际执行回读。

接口读取失败立即清空旧来源展示，并提示重新读取；不回退到以前的人工阈值。读取时间与稳定业务版本分开，只有本站业务内容变化才改变来源版本。参数保存与接口读取互不替代；上游不可用时仍可保存其余人工参数。

### 剩余 13 项人工参数

| 分组 | 字段 |
|---|---|
| 储能能力（5 项） | 可调度容量、最大充/放电功率、充/放电效率 |
| SOC（5 项） | 安全上下限、推荐上下限、期末相对初始 SOC 允许偏差 |
| 购电边界（1 项，可选） | 独立购电功率硬上限，留空表示不另设 |
| 成本与有效期（2 项） | 循环吞吐成本、实时数据最大有效期 |

- 当前 SOC、可用状态与采样时间由实时输入提供，不允许人工替代。动态可调度能力尚未接入；容量/最大功率仍明确为人工边界。
- 未配置数值留空；效率以百分数填写，接口转为 0–1 系数。期末偏差为百分点。
- SOC 推荐范围须在安全范围内。越界或过期柜不参与本轮请求；全部柜不可用时不能构造有效优化请求。
- 保存带版本及并发检查，不覆盖其他窗口的新参数。旧 SQLite 文件仍可读取，旧需量/反送字段只在读取时排除，不重写历史文档；再次保存不接受这三个已移除字段。
- 保存后旧版本候选失效，需用新参数重新计算；不修改候选快照或下发设备指令。账单抽屉保持原样。

## 本地运行

在仓库根目录执行：

```bash
.venv/bin/python -m uvicorn m4.settings.api:create_app --factory --host 127.0.0.1 --port 8844
```

打开 `http://127.0.0.1:8844/m4`。默认数据库 `runtime/m4/settings.sqlite3`，可用 `M4_SETTINGS_DB` 覆盖，已加入 Git 忽略。

上游固定读取 `https://vifa.hlszh.com/api/`。服务端优先使用 `M4_NOCOBASE_TOKEN`；未设置时只解析现有 `.local/energy_efficiency_local_config.py` 的 `EMU_TOKEN` 常量，不执行配置文件、不把凭据传给浏览器。显式设置空 Token 可关闭上游读取，本地参数维护仍可用。当前仅为本地服务；上线前另行接平台认证与部署，不直接用于生产。

上游均使用字段白名单和 GET 分页，禁止重定向，限制单页响应大小、超时和页数。需量、预测、电价及完整实时状态是必需来源；未启用的防逆流和现行计划读取异常仅产生展示警告，不阻断其他必需来源。缺记录、重复记录、非法数据和分页异常均明确报告，不猜测值。

直接双击 HTML 不具备参数服务或上游凭据，必须通过本地服务读取/保存；不把浏览器缓存当作已保存配置。

## 本地接口

- `GET /m4-api/stations/{station_id}/settings`
- `PUT /m4-api/stations/{station_id}/settings`
- `GET /m4-api/stations/{station_id}/control-sources`
- `GET /m4-api/stations/{station_id}/inputs`
- `POST /m4-api/stations/{station_id}/candidates`
- `GET /m4-api/stations/{station_id}/candidates`

电站仅允许 `station-1`、`station-2`，分别映射 `ES01`、`ES02`，未知站返回 404。控制来源只支持读取，无远端写入接口。

参数返回包含 `station_id/revision/version/updated_at/capability_source/parameters`；未配置时 revision=0，parameters/version/updated_at=null。保存请求为 `{"expected_revision": 当前版本, "parameters": 完整对象}`；冲突 409、参数错误 422、存储失败 503。上游读取失败 502。

```text
energy_capacity_kwh, max_charge_kw, max_discharge_kw
charge_efficiency, discharge_efficiency
soc_min_pct, soc_max_pct, preferred_soc_min_pct, preferred_soc_max_pct
terminal_soc_tolerance_pct, grid_import_limit_kw
cycle_cost_per_kwh, max_input_age_seconds
```

控制来源返回原精度数值、来源电站、读取时间、稳定版本及每日计划。`status=ready` 表示必需控制来源可用；`reverse_flow.enabled=false/effective_limit_kw=null` 表示防逆流配置未启用，读取失败时 `reverse_flow=null` 并单列警告。`source_health` 区分每日计划为空与读取失败。

## 真实输入核对

默认页面读取上述 `inputs`，显示未来 24 小时输入覆盖、各柜 SOC/配置容量/参与状态和具体阻断原因。点击“刷新输入”重新读取；保存本站参数后自动重取。定时检查页面所持快照是否过期，但不定时重复拉取七日历史。配置或控制来源版本变化、刷新失败、跨过计划起点、实时数据超出有效期时，不再显示旧输入为就绪。

- **时间轴**：北京时间下一个 15 分钟边界开始，连续 96 点。取数跨过边界时要求重新读取。
- **负荷**：读取 M3 已完成的 15 分钟 `station_total_load`，按每个时间点选择已完成的最新任务。今天和明天可来自不同任务；缺明天就保留缺口，不复制今天、不创建上游任务、不使用 M3 SOC 预测替代初始 SOC。支持 NocoBase numeric 返回小数字符串。
- **光伏**：ES01 固定为 0；ES02 使用读取时刻之前七个完整自然日的 `t_es_data.ac_solar_power`。先每分钟平均、再每 15 分钟平均，最后取七天同时段中位数。每区间至少 12 个有效分钟，连续缺失不得超过 1 分钟；缺失不填零。标注“历史参考”，不是天气预测。
- **电价**：两站共用 `t_peak_diy` 和 `t_rate`，谷/平/峰分别映射 vprice/fprice/hprice。末段 `23:59:00` 按用户确认延续至 `24:00:00`；重叠、缺口和未确认枚举会阻断。96 点同时保留真实时段标签，价格和标签共同进入来源版本；15 分钟内跨价格或跨标签时拒绝无依据折算。
- **SOC**：ES01 固定 emu11/12，ES02 固定 emu21～26，排除 emu27。配置总容量按原固定柜数等分，每柜份额不因排除其他柜而增加；调度初始 SOC 仅对参与柜的 `t_emu.latest_soc` 按其配置容量加权。完整全站 SOC 和上游 `t_es_data.emus_soc` 单列作对照，不能替代参与柜 SOC。
- **设备可用性**：柜及 BCU/PCS 处于待机、充电、放电且没有告警/故障才可参与。任意柜停机、离线、故障、调试、未知、告警、SOC 异常或数据缺失/重复/过期，只排除该柜，其余正常柜继续参与。没有任何参与柜时才因设备状态阻断本站。当前保留 t_emu.alert_status 的原映射，字段与 M1 运行状态含义仍有区别，不把排除标志视为已确认现场故障。
- **可用能力**：配置总容量、最大充电及放电功率都按原固定柜数等分，再合计参与柜份额。当前逐柜功率缺少独立额定/动态来源，因此明确是人工配置等分折算，非实测动态能力。原配置不改，需量阈值和购电边界不按柜比例缩小。
- **时效**：逐柜时间全部校验，调度快照只使用参与柜中的最旧时间；同时满足读取时刻和现有核心的“相对计划起点有效期”。若有效期配置比距下个刻度的时间短，需临近起点再刷新，服务不会自动放宽有效期。

返回包括 `status/issues/warnings`、配置版本、计划范围、分来源状态/版本以及可用时的 96 个标准点。只有完整、当前且至少一柜可用才为 `ready`。局部源失败仍返回其他来源的核对结果；取数期间参数被另一窗口修改时返回 409，要求刷新。

### 凌晨谷段优先早充

用户已确认同价谷段优先早充，三类真实候选在既有目标之后增加末级偏好。按真实“谷”标签识别自然日零点起的连续谷段，在同价区间内保持充电总量、全部放电和区间外充电不变，尽量提前充电；仍须满足需量、购电功率、SOC 和设备能力约束。未完成或未证明早充层最优时保留可行计划并显示提示。无需新增人工调度参数，也不修改已保存配置。

2026-09-07 21:38 本地真实输入验证：电站 1 三类候选均从 09-08 00:00 以 100 kW 开始充电，该点计划购电 111.339 kW。整个候选保守标记 `feasible`，早充末层已完成并证明最优；仅作预览，EMS 未下发。

## 优化器适配边界

`request_from_inputs(configuration, bundle, profiles, now=...)` 将已校验真实输入交给现有 `build_request`，再次检查站点、参数版本、当前时效、逐点值与来源序列一致性，以及参与名单、柜级状态、有效期、可用容量/功率和参与柜 SOC 一致性。局部柜问题不再阻断其余柜；不满足整体请求条件时拒绝。

- 容量/功率由人工参数按参与比例折算，效率及安全边界维持原配置，初始 SOC/时间取参与柜实时状态，需量直接取 `need_kw`；免调功率不擅自扣除。
- `LiveStationState.participating_cabinet_ids` 必填，拒绝空可用名单、重复、未知或异站柜；参与与排除名单、隔离政策进入请求版本和审计信息。数学核心仍输出站级汇总计划，适用范围是参与柜；后续 EMS 分配必须遵守该范围，排除不等于向该柜发送停机指令。
- 防逆流未启用，不设置最小正向购电或 40 kW 外送边界。现有数学核心只允许光伏归因外送；适配以最大光伏输入提供其已有物理界的有限上界，售电价格为 0，不计未经确认的外送收入。
- 配置、控制政策、实时快照、负荷、光伏、电价版本随请求保留；当前能力仍为人工边界。
- 本轮不修改阶段 A 数学核心或 B1 契约。HTTP 提供参数保存、输入读取及数学候选计算，未接 AI、滚动任务或 EMS 下发。

## 启动候选计算

在本站参数已保存、真实输入校验通过后，点击“启动候选计算”。服务端重新获取本站真实输入，并按当时的参与柜范围和可用能力生成均衡、节费优先、光伏消纳优先三类候选。每个可用候选为未来 24 小时、15 分钟粒度的 96 点；点击候选仅切换功率/SOC/指标预览，不能选择为执行计划。

请求体仅为 `{"configuration_version":"页面当前参数版本"}`。不接受客户端提交预测、SOC、柜名单、目标权重或计划。成功响应 `m4-live-candidates-v1` 包含本站真实 `inputs`、严格 `request`、单站 B1 `result`、目标模板元数据、生成时间与有效期。B1 选择状态固定 `pending_selection`、下发状态固定 `not_dispatched`，相关标识为空。

开始新计算清除本站上一轮结果；缺参数/输入不全返回 422，参数或控制版本改变/求解跨过有效期返回 409，本站重复计算返回 429，上游复核失败返回 502。另一站不受本站计算失败影响。求解每类最多 30 秒，三类合计约 90 秒，另计取数时间；超时但有独立验证通过的解显示“可行解”，没有有效解则不展示计划。

GET 返回本地服务进程内最近结果或 null；重启服务后不保留结果。已过计划起点、输入超过有效期或参数已变更，返回 `stale=true` 及原因，仅可作为历史预览。页面还会核对控制版本、刷新状态及晚到响应，避免旧结果覆盖当前操作。预览不是设备许可快照的持续监控。

### 三类目标的调试配置

`m4/settings/objectives.py` 提供全局默认模板及按站部分覆盖，独立于 13 项调度参数，不能由浏览器修改。目标顺序沿用已确认规则：三类均先优化需量超限峰值、持续量；均衡随后为 SOC 推荐范围偏离、归一化费用/PV 未利用量综合、吞吐；节费和光伏类分别优先费用、PV 未利用量。

当前数值为明确标注的 `candidate-debug-v1` 调试默认：均衡综合层分别按 1000 元、1000 kWh 参考尺度归一化，各权重为 1；目标锁定绝对容差为 1e-6，相对容差为 0。不是生产调优结论。配置版本和内容哈希进入每类 profile/plan 版本，完整目标及尺度随结果返回，后续可依据真实候选效果调整。安全 SOC、功率、参与柜范围等约束不会因目标优先级下降而放宽。

费用差额使用同一输入的“无储能购电”基线比较，只是本次预测与配置下的计算结果，不代表实际执行收益或月度账单节费。光伏仍为已确认的历史参考来源，站 1 无光伏时三类候选可能相同，不人为制造差异。

## 验证

针对参数存储、只读分页、真实序列、设备阻断和请求适配运行单元/集成测试；浏览器覆盖参数保存、并发冲突、两站隔离、失败清空、快照过期和明暗模式响应式布局。具体本轮结果与现场缺口见《真实数据接口盘点》的最新条目。

本轮按柜隔离验证：60 项相关后端测试通过，包含真实优化器在剩余柜功率上限内实际充放电的场景；两站真实只读核验显示电站 1 由 emu12 单独参与，可用 522 kWh、100/100 kW、参与 SOC 2.8%，全站 2.4% 仅作对照。


### 有效计划显示

首页只展示按负荷、光伏、SOC和约束计算并校验的优化计划或EMS模拟计划。原始EMS时段保留在调度设置的来源信息中，不再回填为主策略和充放电时间线。缺有效输入时显示“有效计划待计算”；SOC到限导致提前停止时按模拟有效时长展示。当前日计划从零点SOC计算，不代表从当前实测SOC重新起算的滚动计划。

### 站级功率与 EMS 分工（2026-09-12）

M4 输出站级储能功率计划，柜间功率分配由 EMS 负责。M4 保留站级优化、SOC和功率约束、参与柜准入校验；不再计算或保存逐柜分配，不推演逐柜期末 SOC。

原柜级分配预览、生成保存、分配历史及对应 API 已移除。此前保存的分配历史文件保留，不作为新计划输入；站级决策历史继续使用。当前页面仍是计划预览，未接入设备下发或执行回读。
