# M4 优化调度控制台概要设计

更新：2026-09-08。当前架构为 **Pyomo 建模 + HiGHS 求解与最终决策**。静态原型中的人工改计划和 EMS 发送演示不作为现行业务契约。

## 职责

M4 汇总负荷/光伏/电价与储能能力，建立未来 24 小时、96 个15分钟时段的 MILP，计算合法候选并按已保存经营偏好求解最终方案。页面展示计划、比较依据、风险和复核证据。

当前交付为本地计划预览，不直接控制 PCS/BMS。EMS 的正式下发、接收与执行回读接口尚未接入；未来 EMS 仍是唯一设备执行者。

![M4 求解器决策与 EMS 边界](M4-EMS对接流程图.svg)

图中合并了逐候选分层求解细节；EMS 虚线是未来接口边界，未实现。

## 建模与决策

1. 校验完整预测、电价、来源版本、时间对齐及参与柜能力；异常柜隔离，全站输入缺失或全部柜不可用阻断。
2. Pyomo 具名变量和约束表达功率平衡、SOC、充放及进出电互斥、末端 SOC、逆流与 PV 策略。
3. HiGHS 按分层目标生成 balanced/cost/pv。先处理需量峰值和累计超限，再处理各经营目标，最后处理吞吐量与可选谷段早充。
4. 独立复验96点计划并重算指标。最终单选 MILP 按需量两层、显式偏好和并列顺序求解；原确定性比较程序只用于独立审计。
5. 选择前后复核实时输入、参与柜、配置/偏好版本和原有效期；变化或过期则阻断，刷新不能延长旧计划有效期。
6. 返回一套最终建议及可展开的三候选比较，保存独立审计证据。

三候选保留是为了保持既有跨方案需量比较，不默认将 balanced 设为唯一计算目标。`optimal` 和 `feasible` 分别展示；数学选择最优仅针对本轮合法候选和显式偏好，不等于全局经营最优。

## 页面与接口

- 运行入口为“运行优化决策”，按钮触发 `POST decision-runs`，后台按站记录状态且保护重复点击。
- `GET decision-runs` 读取阶段与完成情况；`GET decision-result` 展示最近结束的历史决策证据。
- 历史结果明确标识检查时间与原有效期，不作为当前现场有效计划；当前候选和独立选择仍经过实时复核。
- 选择偏好沿用版本与并发保护。电站1保留用户确认的 balanced/cost/pv 顺序；电站2缺参数或偏好时保持阻断。
- 新编排契约为 `m4-orchestration-v2 / pending_selection`，最终选择独立返回；旧 v1 编排契约不再接受。
- AI 代码、配置、接口、历史评测及原始记录已清除；运行链只使用数学求解与独立复验。

## EMS 对接边界

未来需要 EMS 统一计划下发、接收结果与执行回读接口。M4 不依赖 EMS 内部模板调用或规则仲裁实现；单柜分配由 EMS 负责。当前所有结果固定 `preview_only / not_dispatched`，不会向真实设备发送指令。

## 说明入口

- [运行与兼容说明](求解器决策说明.md)
- [详细架构与约束](docs/m3/specs/2026-09-08-m4-solver-decision-design.md)
- [数学优化器](../docs/m4/optimizer/README.md)
- [最终数学决策](../docs/m4/selection/README.md)
- [经营偏好配置](选择偏好配置说明.md)
