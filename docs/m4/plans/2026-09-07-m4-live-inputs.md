# M4 真实输入接入实施计划

依据用户已确认的规则，落实第一步真实数据适配。仅在本地读取并校验输入，保持数学核心和 B1 契约，账单不改，无生产写入、无 EMS 下发。

1. 控制来源：需量读取 need_kw，不扣免调值；re_flow 仅展示未启用值，不作为方向/功率约束，不再以待确认状态阻塞。每日计划仍为只读参考。
2. 实时状态：固定每站储能柜白名单；容量由本站配置均分，再加权柜级 SOC。完整性、有限值、源时间与安全边界逐柜校验；待机/充放电且无故障告警才可参与。任一异常、缺失、过期或未知状态拒绝本站新请求，不降额猜测或改变 EMS 计划。
3. 电价：两站共用；将最后一段 23:59:00 按已确认语义归一为 24:00；完整覆盖各 15 分钟区间并拒绝缺口/冲突。
4. 光伏：ES01 固定为零，ES02 用已盘点的过去完整日交流光伏历史构造明确标记的历史参考基线。保留来源、质量判断及版本，不声称存在天气预测。核心内不补点。
5. M3：复用现有负荷预测结果契约，读取真实成功任务并验证未来96点的覆盖、粒度和时间轴。缺失或访问失败如实报告，不能回退离线制品或 Mock。
6. 本地 API/页面：展示真实输入与阻塞原因，保留三表、参数保存和既有页面行为。先验证输入，可用性未通过不调用优化器。
7. 验证：新增核心输入规则先写失败用例再实现；测试站隔离、配置加权、过期/异常阻断、末分钟/跨日时间、PV零值与缺点、M3对齐及来源失败。真实接口只读检查、明暗响应式浏览器验证，独立审查关键规则。

## 完成记录

上述七项已完成。107 项后端测试、四组浏览器回归、两站真实输入只读核验及响应式明暗渲染检查通过。独立审查发现的数值字符串兼容、过期输入请求、来源值一致性以及参数/控制刷新竞争已修复。

现场当前阻断：ES01 emu11 活动告警；ES02 本地参数未保存且缺少 09-08 00:00 之后负荷预测。输入阶段已接通，下一阶段为真实候选计算与展示，不代表已完成 AI/EMS 或生产部署。

2026-09-07 后续变更：用户改为按柜隔离，第2项原全站阻断约定由 `2026-09-07-m4-cabinet-isolation.md` 覆盖；正常柜按剩余能力继续参与。
