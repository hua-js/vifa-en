# M4 凌晨谷段优先早充

用户确认：凌晨谷段优先早充。范围为本地真实输入候选计算，不接 AI、不下发设备计划。

## 规则

- 使用电价接口真实 `gu` 标签识别自然日 00:00 起的连续谷段，不用最低价猜测谷段；仅在同价连续区间内重排。缺少标签、全天无时段边界时不启用。
- 原六项目标与所有设备、需量、SOC、功率约束保留；新增可选、独立、末级的充电时延最小化目标。历史请求兼容。
- 末级求解固定所有放电和目标区间外充电，并锁定各自然日同价谷段的充电总量，避免增加循环或跨日、跨价格转移电量。
- 时间不足沿用上一层可行解，明确早充偏好尚未完成。

## 实施与验证

1. 测试先行：请求兼容与目标顺序、真实标签、滚动跨日、同价早充、功率/SOC约束、区间电量守恒、超时回退。
2. 输入适配保留并校验标签，来源版本包含价格与标签；核心增加末级求解与动态锁定。
3. 更新候选目标版本及页面已有参数说明，不改变已保存电站配置。
4. 执行相关核心、输入、候选服务回归与独立审查；重启本地服务，用当前真实输入确认凌晨曲线及候选仅预览。

状态：已完成。

- 测试先红后绿；M4 全量 283 项通过，最终独立求解状态细化后 46 项相关回归通过（含新增的前层可行/末层最优案例）。
- 独立审查发现末层超时有解提示遗漏，已修复并复核通过；额外 18 个滚动窗口/分价/效率/PV 场景通过。
- 本地服务已重启，2026-09-07 21:38 页面实际计算成功；站 1 三套候选均在次日 00:00 充电 100 kW，计划购电 111.339 kW，独立约束复验通过。整体为可行解，早充末层已证明最优。页面已定位零点；没有生产写入、参数变更或设备下发。
