# M4 滚动光伏预测接入实施计划

**目标：** 电站2滚动优化使用最新完成的实际光伏预测，电站1保持零光伏，自然日比较保留历史参考。
**架构：** 复用 NocoBase 只读客户端，以独立 pv_forecast_source 适配器读取 energy_pv_forecast_runs / energy_pv_forecast_points。完整核验96点源批次，再按负荷决定的95/96点时间窗取值；不拼批次、不补点、不触发预测。
**约束：** 沿用查询服务7200秒新鲜度门槛；过期、缺失、错误阻断；记录批次和来源版本。用户已确认执行，沿用不运行测试要求，本轮不部署、不下发、不启用定时任务。

- [x] 添加只读表白名单与适配器：站点/状态/类型、时间顺序、15分钟连续96点、关联ID、有限非负功率、批次版本检查；缺覆盖提示手动更新预测。
- [x] live_inputs仅滚动入口改用预测；自然日继续调用原_pv。按实际95/96点重算覆盖，生成请求时复查来源与时效。
- [x] 同步线上HTML、Node-RED模板及Flow中的光伏来源/更新时间文案，保留自然日参考标签。
- [x] 源码检查与交接文档记录，明确未运行测试、未部署；后续需更新M4后端与页面。

完成记录：Python AST语法检查通过；两份线上HTML及Flow模板一致。未运行测试、真实API请求、求解或部署；未构建/推送M4镜像。
