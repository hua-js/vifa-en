# 电站2未来24小时光伏功率预测

**当前交付：** 生产提供每日06:00及手动预测入口，详见 [生产手动 Flow 与部署说明](pv/README.md)。2026-09-17本地已接入预测前增量更新训练数据，已完成本地专项与只读刷新验证，尚未构建或部署；线上当天读取到的训练数据仍截止9月9日。

## 凌晨日计划的双时段预测（2026-09-19，本地未发布）

用户确认M4自动等待当天负荷预测生成后读取并生成日计划，不要求零点立即产出。M3光伏新增北京时间22:00生成，保留06:00；预测仍为下一刻钟起24小时、96点。前一晚22:00批次约覆盖22:15至次日22:15，次日白天完整，日计划最后7个夜间点由M4现有20:00–07:00缺口规则补零；06:00批次供后续更新。日计划仍须通过负荷评分及其他输入校验。

调度状态按06:00/22:00分别保存最后尝试日期；旧`last_attempt_date`仅作为06:00去重记录迁移。同一时段先持久化声明再提交，失败、崩溃及重启不自动重放不确定写入。保留串行任务互斥；若整个触发分钟繁忙或服务离线，会错过该次触发，不跨时段盲目补发。`PV_DAILY_SCHEDULE_ENABLED`继续控制调度。

M4无需修改业务代码：后台启用时自动重新读取输入，同一15分钟窗口间隔60秒最多3次，耗尽后下一窗口继续；不是负荷完成事件立即触发。前一日批次有效期和夜间补齐校验均保持。已完成21项不同用例验证：调度升级、双时段/跨日/重启去重、不确定提交防重放、白天缺测阻断、22:00批次跨日接入、M4等待输入后恢复重试。日志`outputs/m3/tests/2026-09-19-pv-dual-schedule/`。仅本地模拟验证，未构建、部署或触发线上任务。

发布必须更新独立PV服务镜像；此前M3 Worker镜像更新不等于PV容器更新。M4无需因此部署。本节调度为待发布实现，下文每日06:00镜像与训练证据为历史状态。

## 持续训练数据更新（2026-09-17）

目标：每日及手动预测共用增量刷新链，使用最近可用实测与历史天气重新拟合，保留批次可复算证据。模型特征、拟合方式及预测窗口不变。

- [x] 预测前从上次成功训练源增量读取ES02光伏实测，重取末尾48小时并保留更早历史；首次使用镜像内种子快照。
- [x] 从上次有效训练末尾前48小时至昨天读取历史天气，按时间合并；新天气缺失时保留已有有效值，不将天气预报充作训练实况。天气快照仅本地保存，不新增历史天气入库操作。
- [x] 校验快照、合格样本和截止时间不倒退，再获取未来天气并生成预测；任何刷新错误终止本次任务，不回退固定旧训练源发布。
- [x] 发布回读成功后原子更新`PV_MANUAL_STATE_DIR/training-state.json`；保留每个任务的训练、天气及来源哈希。命令行训练状态锁与服务串行任务共同防止并发覆盖。
- [x] 用户确认后完成增量合并、天气延迟、刷新失败阻断、状态续接及发布失败不推进指针等专项验证。
- [ ] 镜像内运行、构建及生产部署仍未执行。

历史天气可能延迟，刷新成功不保证样本截止到昨天；每批以`training_end`、`training_rows`和来源记录为准，不以任务日期代替数据新鲜度。部署需允许读取`t_es_data:list`并访问历史天气API，持久保留manual状态及其jobs目录。此改动不包含当天实时误差校正，也不保证预测准确率必然改善。

预测模型为 **WeatherRidge 岭回归**。沿用已验证的9个天气交互特征、训练集RMS缩放、无截距、lambda=0.01；目标是十五分钟平均交流功率，单位kW。训练样本来自已保存的ES02实测快照与历史天气交集。

## 联合验证结果（2026-09-17）

- 真实只读刷新在`outputs/m3/validation/20260917-continuous-training/training`完成：原始实测82,710行，本次增量27,282行，合格训练点2,136→2,812，训练截止9月9日22:00→9月16日23:00。未调用天气入库、预测发布或线上任务接口。
- 49项相关用例通过：增量刷新、状态首次初始化/重启续接、旧版升级、状态损坏/丢失阻断、发布失败不推进指针、日调度、模型与发布校验等。新链的状态文件丢失时，若已有带`training-refresh.json`标记的发布工件则阻断，不能静默回种子；旧版无标记任务允许首次升级。发布与本地指针不是跨系统原子事务，不确定发布仍需人工回读。
- 离线联合回放入口`scripts/replay_pv_training.py`：逐批按预测当天00:00前1小时截断训练标签，使用原批次保存的天气预报。9月10–16日、2212个滚动目标对；两组时间、批次及实测集合经独立审查确认相同，脚本另加集合一致性断言。

| 同样本方案 | 平均绝对误差kW | 平均正向误差kW |
| --- | ---: | ---: |
| 旧固定训练，不校正 | 74.16 | 36.14 |
| 旧固定训练，保守校正 | 68.41 | 26.82 |
| 持续更新训练，不校正 | 71.47 | 40.15 |
| 持续更新训练，保守校正 | 64.61 | 29.31 |

校正对更新模型仍有效；但新模型加校正相对旧模型加校正的正向误差增加2.49kW，不能宣称所有指标改善。当前不建议仅凭MAE下降直接发布联合版本，需要进一步解决高估退化或取得明确的取舍决定。

这是使用事后历史再分析天气的回溯敏感性评估：天气可能修订，遥测到库时间不可还原；虽排除目标日训练标签，仍不是严格历史在线复现、独立留出验证或生产验收。报告：`outputs/m3/validation/20260917-continuous-training/replay/summary.json`；其中短时校正对新模型的门槛通过，与旧模型加校正相比的无退化条件未满足。此次未调校正参数、未构建推送或部署。

## 计算与追溯

- 使用实际运行的 `as_of`，从其后下一个十五分钟开始输出96点；`forecast_end` 为不含的右端。恰好22:15运行时从22:30开始。
- 新输入必须在 `as_of` 前读取完成；实际使用功率样本及天气 `fetched_at` 均不晚于 `as_of`。未知天气发布时间继续为NULL。
- 训练至少包含7个完整日，每个实测十五分钟至少12个有效分钟，四个合格点才纳入小时。训练数据不包含标签结束晚于 `as_of` 的行。
- 训练和预测共享天气对齐函数：辐照取小时结束标签，温湿度/云量/风速插值到区间中点，降水均分四份。整批天气需要50个连续小时标签，并完整覆盖新确定的目标窗口；不混批次、不用零填补缺失。
- `raw_forecast_kw` 保留约束前结果，`forecast_kw` 将负值截为零，`is_clipped` 记录是否发生截断。写入numeric(14,6)，四舍六入五成双至六位小数；原始模型参数和输入保留可还原的浮点字符串。
- 没有确认交流额定容量，因此不假定上限。七日参考 `baseline_kw` 和预测区间上下界保持NULL；当前没有生成七日参考或估计置信区间。
- 批次 `source_manifest` 内嵌实际使用的聚合训练样本、完整预报天气快照、输入文件SHA与来源路径、聚合规则、运行环境；`model_config` 内嵌特征、缩放和系数。无需依赖原实测表后续是否变化，即可复算该批预测。
- 历史第一批训练截止2026-08-31 23:00，1,276点；9月9日种子训练源为2,136点。持续更新版从种子或上次成功源续接，具体样本截止时间由每批记录确定。预测精度须以发布后的实测评估。

## 运行命令

在 `vifa/` 目录执行，Python需已有NumPy/Pandas。每次使用新的快照和输出目录；已存在目录拒绝覆盖。

```bash
python3 m3/scripts/fetch-open-meteo-weather.py --fetch-dir /absolute/path/weather-snapshot

PV_NOCOBASE_IMPORT_API_KEY_FILE=/absolute/path/private-key-file \
  python3 m3/scripts/fetch-open-meteo-weather.py \
  --snapshot /absolute/path/weather-snapshot --execute

python3 m3/scripts/generate-pv-forecast.py \
  --weather-snapshot /absolute/path/weather-snapshot \
  --output /absolute/path/pv-forecast-bundle

PV_NOCOBASE_IMPORT_API_KEY_FILE=/absolute/path/private-key-file \
  python3 m3/scripts/publish-pv-forecast.py \
  --bundle /absolute/path/pv-forecast-bundle --execute
```

凭据文件遵循运行服务的只读权限要求；只向固定NocoBase域名发送认证。所需权限：天气表list/create、预测批次表list/create/update、预测结果表list/create。工具没有数据库直连、设备控制、调度或定时运行入口。

`generate-pv-forecast.py` 默认使用第一轮回测已验证的永久输入快照，可通过 `--backtest-dir` 指定同格式工件；使用刷新训练源时指定 `--training-source /absolute/path/pv-training`。生产手动服务已显式指定包内刷新源。生成器不访问网络；导入器默认仅本地核验，只有 `--execute` 才写入，`--verify-only` 只读。

入库先预检三表和引用天气批次，再创建 `running` 批次，补写缺失结果。逐字段回读96点、确认时间/关联/值一致后，才更新 `completed`，随后再次回读。`content_hash` 覆盖模型、来源、运行时间与96点内容；运行状态和哈希字段自身不参与哈希。

相同工件重放会按 `run_id` 查已有数据，一致则跳过，冲突拒绝覆盖。POST不自动重试；异常时先回读实际状态，未完成的批次保留 `running`，不会被正常查询选中。首次发布或恢复尚未完成批次时，目标窗口若已经开始，拒绝继续，需生成新的未来批次。已完成批次允许事后只读核验。NocoBase多次请求不是数据库事务；这些完成门槛由本写入服务保证，不约束绕过该服务的其他写入方。

为保留首次执行证据，重放时另存报告：

```bash
PV_NOCOBASE_IMPORT_API_KEY_FILE=/absolute/path/private-key-file \
  python3 m3/scripts/publish-pv-forecast.py \
  --bundle /absolute/path/pv-forecast-bundle --execute \
  --report /absolute/path/pv-forecast-bundle/idempotence_verification.json
```

## 通过NocoBase查询

先查询 `GET /api/energy_pv_forecast_runs:list`：

```json
{
  "filter": {
    "es_sn": {"$eq": "ES02"},
    "run_kind": {"$eq": "operational"},
    "status": {"$eq": "completed"}
  },
  "sort": "-as_of",
  "pageSize": 1,
  "fields": "id,run_id,es_sn,as_of,forecast_start,forecast_end,interval_minutes,expected_points,status,model_name,model_version,training_end,weather_batch_id,generated_at"
}
```

上述为查询参数表示，`filter` 在URL中需JSON序列化并编码。取返回的 `id` 作为 `run_pk`，查询 `GET /api/energy_pv_forecast_points:list`，`filter={"run_pk":{"$eq":实际id}}`、`sort=target_time`、`pageSize=100`。界面展示查询限定字段即可，复核模型时再读取较大的 `source_manifest/model_config`。

## 本地产物

`run.json`、`points.json` 是可复算的待发布工件；其中run的completed为最终目标状态，是否实际入库必须看 `publication_verification.json`，不能从本地run状态推断数据库状态。`summary.json` 描述生成时状态；实际入库报告另存，保留生成证据不回写。

另有 `forecast_points.csv`、`training.csv`、`forecast_weather.csv`、输入副本、源码与SHA清单。`plot-pv-forecast.py` 读取成功核验记录后导出静态功率/辐照曲线，需要Matplotlib和中文字体；当前使用临时绘图依赖，未增加生产依赖。

运行时通过业务表 list 检查字段可读性，不调用 collections:get；空表允许首次写入，完整字段和值仍由入库回读校验。字段类型、索引和约束由独立建表工具检查。
