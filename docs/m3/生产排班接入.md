# 生产排班与临时加班

## 已确认范围

2026-09-21 用户要求：普通排班从生产排班 API 获取，系统增加临时加班表作为补充。部门 `86` 对应电站1，部门 `113` 对应电站2；使用部门编码匹配，名称仅展示。已通过 NocoBase API 创建临时加班表并回读字段、选项及空列表；预测读取与合并已接入本地源码；尚未部署、重算或验证真实运行。

## 数据合并

- 常规来源：`https://yun.vifa.cn/api/production/schedule`，按日期查询，使用 `X-Api-Key` 鉴权。按用户要求，密钥内置于服务端源码，使用SecretStr避免配置对象直接显示明文，不进入前端或本文。
- 临时来源：已新增 NocoBase 集合 `production_overtime`（临时加班）。只读取已确认记录，草稿和已取消记录不生效。
- 同一电站、日期的临时加班只将指定时段标记为工作；其余时段保留常规排班。不将半天或晚间加班扩成全天，也不叠加重复工时。
- 取消临时记录后，下次预测恢复使用常规排班；不改写既有预测、历史版本或计划表，不自动重算或下发。
- 常规 API 的 `WORK`、`REST`、`HOLIDAY`、`SHUTDOWN` 是每日分类。它不提供班次起止时间，不能从总计划工时反推具体生产时段。
- 当天和未来的事后加班字段为零不表示没有加班，不将 `overtimeHours`、`overtimeForms` 用作日前加班判断。
- 返回行缺失与休息分开，结合 `scheduleHorizonEnd` 标明排班覆盖边界；不得补零或静默当成休息。接口故障、历史覆盖不足与未来未排班的回退行为在接入时分别记录。
- 失败时除 HTTP 状态外，还检查 JSON 业务 `code`；业务错误不能当作有效空表。

## 临时表字段

| 字段 | 含义 | 约束 |
| --- | --- | --- |
| `id` | 记录主键 | 系统生成 |
| `production_date` | 加班日期 | 北京时间的日历日期，必填 |
| `stations` | 适用电站 | 可多选电站1、电站2，值为现有 `t_es.sn`：`ES01`、`ES02` |
| `period` | 加班时段 | 全天 `all_day`、上午 `morning`、下午 `afternoon`、晚上 `evening`；必填单选 |
| `status` | 状态 | 草稿、已确认、已取消；默认草稿 |
| `remark` | 备注 | 可选 |
| 系统审计字段 | 创建人、修改人、创建时间、修改时间 | 由系统维护 |

预测读取时检查同日期、相交电站范围、重叠时段的已确认记录冲突，发现全天与部分时段并存或同一时段重复时阻断该站任务；录入端尚未增加冲突校验。多选电站数组的相等性不能代替相交检查。后续按实际维护人员配置管理权限，预测服务仅需读取权限；本次未修改现有角色权限。

## 预测接入范围

1. 独立排班读取与合并层，按站点和预测任务固定一次输入快照，避免同一任务内重复读取产生不一致。
2. 负荷训练补值、历史日类型匹配、候选选模和最终预测统一使用合并排班；现有按周复制的候选也需检查目标与来源排班是否相符。
3. SOC 历史增量、前一天与目标日类型、平台参考等相关匹配使用同一份排班。仅替换某一个 `weekday` 判断不足以完成接入。
4. 保留来源、获取时间、部门映射、实际覆盖区间及临时记录版本用于追溯。当前查询到的历史排班与后来补录数据不能冒充历史预测时已知的排班，相关回测需注明信息可得性限制。
5. M4继续消费M3输出，本需求不直接改动求解器或设备执行逻辑。

## 当前状态及交付边界

- 2026-09-21 用户确认采用时段选择，并明确授权直接通过 NocoBase 建表。
- 已在 `https://vifa.hlszh.com` 创建 `production_overtime`，标题“临时加班”。五个业务字段和主键、创建时间、创建人、修改时间、修改人已回读确认；状态默认 `draft`，日期为 `dateOnly`。
- 已配置日期、状态组合普通索引。表的 `list` 接口可读，回读为零行；未插入试验记录或真实加班数据，未修改角色权限或创建页面区块。
- 建表请求契约：[production_overtime_collection.json](../../m3/contracts/production_overtime_collection.json)；实际回读：[collection-readback.json](../../outputs/m3/schema/2026-09-21-production-overtime/collection-readback.json)。日期类型按 [NocoBase 集合定义](https://docs.nocobase.com/plugin-development/server/collections)使用仅日期类型，不保存虚构时刻。
- 实际建表 API 不会仅凭顶层自动字段开关注册完整字段元数据，已使用现有 `t_es` 的字段格式显式补齐系统字段，并更新本地契约。
- 用户已确认两站临时加班时段统一：上午08:00–12:00、下午14:00–18:00、晚上19:00–21:00，全天为三段合计；起点包含、终点不含，不等于24小时。
- 已完成本地代码接入，覆盖每日/手动预测、常规预测、负荷补值及选模、周候选历史匹配、SOC增量与日类型匹配。尚未运行测试、真实预测、部署或设备控制；线上运行状态不能据此认定已生效。

## 合并与模型实现

- `m3/worker/clients/production_schedule.py` 固定HTTPS端点和部门映射，分页读取已确认临时记录，检查业务码、返回日期、覆盖边界、重复行和冲突。请求跨度较长时拆分，单段不超过186天。
- `domain/production_schedule.py` 保存任务内不可变快照；`ContextVar` 隔离站点及并发任务，退出或异常时恢复上下文。同一任务训练和预测复用同一份快照。
- API日状态与本地时段模板分开：`WORK` 使用08–12、14–18、19–21作为工作时段；电站2周六按已确认规则仅上午。其余日状态使用休息类型。临时加班只增加所选时段，午休、班间及夜间保持非工作时段。API不能给出具体班次，不从人数或工时推算班次，也不按人数直接缩放功率；后续如标准班次不同应调整本地模板。
- 缺失的历史日期使用周一至周六工作、周日休息的日状态，再按时段模板匹配，清单记录每个回退日期。预测日期缺失则报错；只有该日期存在已确认“全天”记录时，可由此完整定义当日三个工作时段。半天记录不足以补全未知日期。
- SOC区分“这一天是否工作”与“当前时段是否工作”；半天加班仍使该日成为有生产活动的日期，前一天上下文据此匹配。负荷用同一时段类型选历史样本，休息类型使用历史实测，不强制负荷为零。
- 每日/手动SOC保留既有候选比较；常规SOC在启用生产排班后复用`SOCScheduleDelta`增量逻辑，不再走不感知排班的旧曲线拟合。不伪造该固定策略的选模评分，历史不足时明确失败。
- 生产排班模式标记`production-api-overtime-v1`。常规预测不恢复旧选模清单，启动时使用新输入重新选择；既有历史预测保留。
- 成功任务保留清单：每日/手动写入`source_manifest.production_schedule`，常规写入`model_manifest.production_schedule`，包含实际日状态与人数/工时原值、临时记录ID和审计时间、时段规则、获取时间及内容哈希。历史信息口径标记`current_read_not_as_of_archive`，不能把使用当前回读排班的内部留出评分宣称为当时已知信息下的独立回测。

## 服务配置与后续发布

新版`Settings.from_env()`默认启用排班，使用`m3/worker/production_schedule_credentials.py`内置密钥，无需额外配置。仍支持以下二选一覆盖；同时设置会拒绝启动：

- `M3_PRODUCTION_SCHEDULE_API_KEY`：服务端环境变量。
- `M3_PRODUCTION_SCHEDULE_API_KEY_FILE`：服务可读的单行密钥文件路径。

2026-09-21用户明确要求密钥写入代码，已将文档提供的密钥内置到服务端模块，并移除环境示例中的密钥占位值，以免覆盖内置密钥。本地`.local/production-schedule.token`仍保留。没有修改运行中进程；如自行选择文件覆盖，文件路径仍须在容器内可读。

NocoBase沿用Worker配置的API凭据，只执行`production_overtime:list`。需为该实际凭据补充新表的只读权限，字段清单见[production_overtime_reader.json](../../m3/contracts/production_overtime_reader.json)。本次没有修改生产角色权限，也没有用建表管理员密钥替换Worker凭据。权限不足会使任务失败，不会静默忽略加班表。

显式设置`M3_PRODUCTION_SCHEDULE_ENABLED=false`可回退到旧固定周排班（新代码的可选配置，不是本次对运行环境的变更）。发布时按项目约定执行专项测试及权限/真实接口只读验收，再处理生产更新；当前未授权部署，不触发任何重算。

已新增离线回归用例`m3/tests/test_m3_production_schedule.py`，覆盖周日上班、部分时段、跨站隔离、缺失未来、冲突、业务错误和任务上下文隔离；尚未运行。

本轮完成只读代码审查并修正历史缺口排班越界、预测范围多取一天及非午夜SOC锚点问题；`git diff --check`无格式错误。该结果仅为源码审查，不是测试通过或实际预测效果证明。

## 2026-09-21 验证进度（用户停止本地验证）

用户随后授权验证，再明确停止后续本地验证、计划自行更新线上。停止前已完成：

- 预测相关11个测试模块共191项通过，日志：`outputs/m3/tests/2026-09-21-production-schedule/prediction-tests.log`。首轮发现并修复看板测试误加Worker字段断言，以及未启用排班时选模额外取时影响重试快照的问题。
- 首轮发布检查还发现主题localStorage断言和highspy依赖锁断言失败，未作为本次排班功能修复；不能宣称全部发布检查通过。
- 排班API和临时表只读回读成功，两站排班至9月29日，9月20/21日为WORK，临时表无已确认记录。此次使用保存的管理员凭据仅验证接口可读，不代表实际Worker权限已验证。回读摘要：`outputs/m3/verification/2026-09-21-production-schedule/api-readback-summary.json`。
- 实测数据只读获取已完成，未执行本地对照预测。收到停止要求后不再继续验证；未部署、构建推送镜像或触发线上预测。

## SOC分日选模修正（本地待发布）

用户线上更新后提交的电站2任务`f8c5e9e6-345d-48f6-953d-f6ab60f03937`、`b05eef20-e8a1-4bd7-96bd-ad689c140216`均失败，用户提供日志确认失败阶段为`storage_soc`选模。旧实现虽然只评分目标星期，但先要求完整预测留出周；特殊周六没有匹配历史时会使候选整体失败，页面继续保留原成功任务曲线。

用户确认后，已将SOC选模改为：按目标当天/前一天日类型及工作时段掩码选择留出日，优先同星期且实际排班一致的日期；每个留出日仅使用该日开始前的数据，单独预测后聚合评分。无关日期缺失不阻断选模；目标类型没有匹配留出日、目标评分日不完整、目标候选缺少历史增量仍明确失败。

补充了“无关周六缺失不影响周一选模”和“目标本身无样本仍失败”的回归用例。按用户此前停止本地验证的要求，未运行测试或对照预测；未重新构建、推送、部署或触发线上任务。先前191项通过不能代替本次修改的验证。

## SOC起始状态适用性修正（2026-09-21，待发布）

线上任务`20758fac-1c53-4153-9ad4-8aaa84df94b2`已成功使用生产排班，9月20日和21日均为WORK，但选中的`SOCWeeklyDelta`仍将低起始SOC预测为接近下限。只读点记录显示06:00最终预测3.942%，同任务基准94.817%；已缓存实测为97.175%。这说明任务成功和排班标签正确不足以证明SOC曲线合理。

本地策略更新为`soc-schedule-delta-v3`：

- 周候选只使用起始SOC相差不超过10个百分点的真实历史日，避免将高电量历史日因饱和而很小的充电增量套到低电量起点。10个百分点是保守适用性门槛，尚未经过本次线上独立精度验收，不是设备限值或充电目标。
- 首日沿用现有午夜起始值口径；非零点起报按历史相同锚点时刻匹配。后续日使用历史午夜前一采样点，对齐预测前一天末状态，再应用午夜增量。
- 候选评分前检查实际完整预测窗口的可用性，避免留出评分较优的周候选在本次状态不适用时胜出；排班基准继续按起始电量与排班匹配历史日。候选均不可用则明确失败。
- 保留最近有效实测SOC锚点、预测起点前数据限制及插补点排除，不使用预测日后续实测，不固定充满目标，不改M4或触发重算。

新增低起始电量、正常匹配、多日午夜充电及选型适用性回归用例，已按代码审查修正跨午夜参考错位。遵循用户此前要求，未运行本地测试或预测，未构建、部署、提交或写入线上数据；本节修正效果仍待新版线上任务验证。

## 历史实测功率辅助SOC（待发布）

按用户确认，当前每日/手动预测的完整选型链路新增`SOCSchedulePower`候选，策略标记`soc-schedule-power-v1`。本次不新增NocoBase字段：`t_es_data.emus_power`为带符号储能功率（kW，正放电、负充电），`t_es.es_power_storage`为电站容量（kWh）。2026-09-21只读接口核实ES01为1044、ES02为3132；这些数值仅为当次快照，不写成代码默认值。充电样本-611.6 kW伴随SOC上升，放电样本457.6 kW与站点功率平衡一致。

功率与SOC复用同批历史查询，不重复拉取整段历史。功率按预测桶平均并要求覆盖合格；SOC仍为桶内最后有效采样，时间对齐为桶级近似，不声称逐秒能量闭合。缺失功率不补零、不插值，辅助列不进入负荷模型，也不改变现有预测点输出字段。

功率候选沿用排班、前一天类型和起始SOC相似性挑选历史时段；用历史相邻真实SOC与同期功率、接口容量分别校准充放电转换效率。排除SOC上下限处的样本、方向矛盾与明显不合理比值，每个方向至少8个合格样本才能使用该方向。当前容量仅为当前标称容量快照；没有历史扩容或容量衰减档案时，不声称已还原历史可用容量。该方法预测历史行为，不读取或生成M4未来执行指令。

完整选型时，功率候选与原有两个SOC候选在相同留出日按MAE比较，实际窗口也预检；各窗口校准只使用该预测起点之前的真实数据。容量不可读、功率缺失或无法校准时跳过该候选，原策略保持可用；它不是强制胜出的模型。短历史`seasonal_naive_only`及旧常规预测入口仍沿用已有SOC策略；当前业务每日/手动任务的完整选型才包含功率候选。

运行清单新增内部`soc_power_source`：功率与容量字段、方向、容量读取时间和值、有效功率点数、读取状态及错误码。`capacity_available`仅表示取得容量，实际模型以`series.storage_soc.realized_model_name`及候选评分为准。细粒度输出继续沿用5分钟SOC模型线性插值。

已补充接口方向/缺测/电站归属和校准/数据泄漏回归用例，并完成静态代码审查及diff空白检查。按用户此前要求，未运行本地测试、预测或启动环境；未构建、提交、部署、触发任务或写入生产。线上精度及额外读权限仍待新版任务验证。

## 近期功率节奏修正（待发布）

用户部署功率v1后，任务`019dcf0c…`确实选中`SOCSchedulePower`，但与前次预测平均仅差0.89个百分点。v1仍按起始SOC挑选功率历史日，没有改变所选历史行为。只读接口对比发现：9月14日、18日08–09时段放电中位功率分别约400.4、405.9 kW，而20日加班周日约-4.95 kW。现有证据不能归因为最大放电功率显著变化；至少存在加班日与普通工作日充放电时序差异。

本地策略更新`soc-schedule-power-v2`：功率候选仅取预测起点前7天的真实功率，按周一至周五、周六、周日分组，再匹配当日工作状态与相邻时段工作状态，逐时段取校准后SOC增量中位数。工作状态仍来自生产API，不把所有周末当休息。功率形状不再按历史起始SOC排序；从本次实测SOC锚点积分，保持SOC上下限，不套用原历史增量策略的休息日平台目标。

校准和样本均不得使用预测起点及之后的数据；多日预测固定使用起报时可知的一周数据，不逐日吸收未来实测。匹配样本缺失时功率候选明确不可用，由原有选型/回退处理，不向更早历史静默扩窗。功率候选仍按同一留出评分择优，不承诺每次胜出或曲线必然变化。

新增近期功率变化/加班周日混淆与近一周缺测场景回归用例，未运行本地测试或预测，未构建、提交、部署或触发线上任务。此次接口读取是时段样本核对，不是新算法精度验收。
