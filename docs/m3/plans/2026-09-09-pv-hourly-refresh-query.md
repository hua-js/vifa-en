# ES02 本机按小时更新与查询实施计划

**最新范围（覆盖原调度计划）：** 用户改为输出生产 Node-RED Flow，确认“手动采集天气、生成一次光伏预测、查询结果”，目前不需要完整小时更新流程。保留已完成的本机单次验证，不创建 Codex automation，不部署生产。

**目标：** 补齐9月可用历史天气、更新实测训练快照，提供最新完成批次查询，并交付没有定时触发的生产 Flow 与配套服务包。

**已确认范围：** 用户选择先在本机运行；继续WeatherRidge，不接页面/M4或远程部署。仅NocoBase接口读写既定表，无设备控制。

**架构：** 已完成 Archive API 补采和最近48小时实测回查，保留长历史。生产版固定这份已核验训练源；Node-RED 手动按钮经私有 Unix Socket 调用独立 FastAPI 服务，异步执行天气采集或单次预测；单进程/任务互斥，失败不自动重放。查询 NocoBase 最新 completed 并验证96点及内容哈希，明确过期状态。原按小时 CLI 保留为已验证工具，未启用调度。

## 约束

- 纬度23、经度113、ES02、Asia/Shanghai，11个原有天气变量/单位不变。
- 补采从2026-09-01到获取日；仅选请求开始时已经结束的小时标签，原始响应含未来标签时保留原文但不写历史行。NULL不补零；不猜测最终气象模型。
- 首批原始历史天气不改写，新批次保留fetched_at和source_metadata。历史训练可跨明确记录的历史批次；未来预测始终只用一个完整预报批次。
- 固定实测上界分页；验证页数、数量、站点、重复、时间边界及功率质量。最近48小时重新读取并取该次快照为准，较早历史保留既有快照；不宣称已自动追溯更早上游修订。
- 每次任务先存快照/工件再发布；写入失败不盲重试POST、不改旧预测。未完成小时允许按已保存工件回读恢复；已经过去的预测窗口需新运行。
- 凭据在仓库外0600文件持久保存供用户已授权的本机任务使用；查询响应只包含展示字段，不泄露凭据或完整训练快照。

## 1. 补采与训练源

- [x] TDD：历史未来标签排除、单位/网格/间断/NULL/精度验证、同快照重放稳定；增量实测合并保持长历史并替换指定回查窗口。
- [x] 新增 `m3/scripts/fetch-open-meteo-history.py`，复用原天气导入器，通过NocoBase保存和核验补采215行。
- [x] 新增 `m3/scripts/refresh-pv-training.py`，实测增至61,189行，得到2,136个合格训练点；真实 hasNext 分页无 count 的情况已复现、修复并回归。
- [x] 生成器增加 `--training-source`，保留原首批调用方式，模型和聚合规则保持；新增训练天气来源清单。

## 2. 按小时运行命令

- [x] TDD：文件锁互斥、相同成功小时跳过、失败不得记成功、状态原子替换、进程中断后不永久锁死。
- [x] 新增 `m3/scripts/run-pv-hourly.py` 并完成单次实际验证；未启用周期任务。
- [x] 新批次 `dba09dce-7f2a-49c1-9358-f91fe7b8b430` / run_pk=2，96点核验完成；同小时重复返回 already_completed。
- 原每小时 automation 项已由用户取消本阶段范围，不创建。

## 3. 最新预测查询

- [x] TDD：只选ES02 operational completed、排序/上界、96点/哈希与时间校验；空表、过期/旧批次和上游错误明确返回。
- [x] 新增 `m3/worker/services/pv_query.py`、`m3/worker/pv_query_app.py`，仅GET `/health` 与 `/api/pv/ES02/latest`，Pydantic过滤响应；同步读取在工作线程执行。
- [x] 本机8850启动查询服务并真实调用；生产版复用同一查询逻辑。
- [x] 54项光伏 Python 测试、7项天气测试通过；保留依赖的既有 Starlette 弃用提示。

## 4. 用户确认的生产手动 Flow

- [x] `m3/pv/pv_manual_production_flow.json`：独立26节点，四个手动按钮（含状态查询）、一个认证只读 HTTP 入口，所有 inject 无定时/启动触发。
- [x] `pv_manual_jobs.py`、`pv_service_app.py`、`run-pv-manual.py`：固定操作、私有认证、互斥与持久任务状态；中断不重放；预测前验证训练源，天气入库核验后才生成/发布。
- [x] 5项 Flow 行为检查及 Compose/入口脚本检查通过；所有固定 curl 命令不拼接 msg.payload，令牌通过 stdin 传入；stdout/rc 按请求ID分组。
- [x] 打包服务、Flow、冻结训练源及SHA清单，不包含凭据。生产 Docker 构建和 Node-RED 导入留待部署验收，不声称已上线。
