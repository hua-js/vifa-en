# 项目约定

- 中文沟通，结论先行；代码、变量名和接口字段使用英文。
- 回复简洁，不复述需求；只说明结果、关键变更、验证和风险。
- 只修改需求相关代码，保留已有改动，不执行破坏性 Git 操作。
- 能从代码和上下文确定的事项自行处理；仅在关键业务口径不明确时提问。

## 模型协作实践

参考 [OpenAI 官方模型指南](https://developers.openai.com/api/docs/guides/latest-model)，核对日期：2026-09-05。以下原则用于本项目后续任务，结合下方开发流程与安全边界执行。

- 以目标、约束和可检查的完成标准推进工作；在已授权范围内持续完成实现与必要验证，不停留在建议或计划。
- 长任务保留原始目标、已完成工作和未解决事项；用户中途补充要求或提问时纳入当前任务，除非明确取消或替换目标。
- 确需额外授权时，先完成不依赖该授权的准备工作，提供具体、可审查的结果，再请求确认；不重复索取已有授权。
- 在系统与开发者指令允许的范围内，用户明确要求优先于 Skill 指南；若 Skill 导致暂停或偏离目标，指出具体文件、原文及适用原因。
- 多代理沿用本项目开发流程与用户明确要求；官方示例不自动改变项目的使用条件。
- 模型选择、推理强度和 API 参数仅在相关配置任务中按当时官方文档核对并验证，不将文档建议视为当前运行环境已启用的能力。

## 项目状态

- 涉及业务功能、核心模块或续接历史任务时读取 `CODEX_HANDOFF.md`；独立小任务无需读取。
- 仅在目标、进度、业务口径、风险或下一步发生变化时更新交接文件。
- 不在交接文件中记录密码、Token、私钥等敏感信息。

## 开发流程

- 纯静态 Mock：直接实现，使用 Mock 数据，不接真实接口，不修改后端、数据库或生产配置；不用 brainstorming、计划、TDD、多代理或 worktree；不要求测试、完整构建或类型检查，但至少检查页面视觉和基础交互。
- 小任务（SQL、Docker、配置、小范围样式）：直接处理，不用 brainstorming、计划或多代理；执行必要验证。
- 普通功能（接口、页面、数据表、业务功能）：需求明确时直接实现；关键业务口径不明确时先澄清。涉及多种方案或较大设计取舍时使用 brainstorming。完成后执行与变更相关的验证，默认不用 worktree 或多代理。
- 核心模块（预测、EMS 调度、需量控制、充放电策略）：涉及算法、控制规则或架构变更时，先明确约束并制定实施计划，按需使用 brainstorming、writing-plans。新增或修改核心规则时采用 TDD；已有缺陷先复现，再修复。重要变更进行代码审查。
- Mock 页面接入真实 API、业务逻辑、状态管理或生产代码后，按普通功能处理。
- Skills 按任务需要启用，不因可用而自动套用完整流程；不重复执行已完成的需求澄清、规划或验证。


## 前端样式

- 创建或修改 VIFA 页面前读取 [VIFA 界面设计规范](docs/DESIGN.md)。它统一颜色来源、排版、组件状态、数据表达与视觉验收；与 `vifa-frontend-style` 的默认值冲突时以项目规范为准。默认尺寸用于新建或正在修改的组件，不要求顺带重做现有页面。
- M3 仅更新页面 HTML 时，直接维护 `m3/node_red/m3_production_gateway_template.html`，不更新 Flow JSON，不生成 page.html 副本，不运行旧页面同步脚本。
- `m3/node_red` 仅保留 `m3_production_gateway_template.html`、`m3_production_gateway_flow.json` 和 `本地机器.txt`；不恢复旧版部署文件或同步脚本，页面维护说明放在 `docs/m3/部署说明.md`。

- M1、M2 的背景样式统一参照 `m3/node_red/m3_production_gateway_template.html`，包括明暗模式下的页面渐变背景、面板底色及配套边框、阴影。这是必须保持一致的视觉基准。
- 交互配色、按钮底色、选中态、分区标识、排版和组件细节可按页面用途自行设计，不强制使用绿色或 M3 的蓝色，也不要求逐项复制 M3。保持整体协调、清晰的业务语义、足够的对比度和状态辨识度；常规设计选择无需再次确认。
- M1/M2 共用主题源为 `shared/styles/vifa-m3-theme.css`，通过 `--m3-*` 变量适配各页面。主题 CSS 内嵌到 HTML 的 `style#vifa-m3-theme` 中，保留单文件部署方式；同步方法见 `shared/styles/README.md`。
- 修改共用主题后，须同步两个实际 HTML 入口：`m1/web/dashboard_energy.html`、`m2/web/场站三条能效链路能流图.html`。不能只修改主题源文件，也不恢复已删除的旧页面副本。
- 保持明暗模式一致，M1/M2 使用 `dashboard-theme` 本地偏好。视觉设计可调整布局和组件细节，但须保留数据逻辑、时间展示和历史查询等业务功能；未经要求不修改作为基准的 M3 模板。
- 使用 `vifa-frontend-style` 技能时，本节背景规范和用户当前要求优先；其余视觉设计可结合技能建议自行处理。
- 验证须查看页面实际渲染，确认背景与 M3 一致，并检查自定义交互配色的可读性、选中态和状态辨识度，覆盖明暗模式与桌面、平板、手机布局；不能仅凭 CSS 变量相同就认定样式已统一。

## 安全边界

- 不直接操作生产环境，授权只读的API接口
- EMS 控制默认只生成建议或模拟结果；未经明确确认，不向真实设备下发指令。
- 未经用户明确要求，不使用 SSH 或连接远程环境。

## 验证

- 按变更风险和影响范围选择必要的测试、类型检查、构建或行为验证，不默认全部执行。
- 必要验证通过后，仅在出现新变更、失败或尚未解决的疑点时扩大或重复检查；低风险改动避免新增只复述实现的测试。
- 只报告实际执行的验证，区分通过、失败和未执行。相关验证失败时不得声称验证通过；因环境限制或已有问题无法验证时，明确说明原因及未确认的范围。

## 临时文件管理

- 探索、调试、一次性验证产生的文件默认不得作为正式项目文件保留。
- 临时脚本和中间结果在任务完成后应删除。
- 有长期价值的测试应整理到对应模块的 `m1/tests/`、`m2/tests/`、`m3/tests/` 或 `m4/tests/`。
- 有长期价值的工具脚本应整理到 `scripts/`。
- 设计探索结果应合并到现有 `docs/` 文档，不创建多个重复方案文件。
- 不得为了记录开发过程而大量创建 Markdown 文件。



## 文件与产物管理

### 正式代码

正式业务代码只能修改现有源码目录。
禁止为了分析、调试或实验复制完整源码到 outputs、reports、tmp 等目录。

### 测试

长期有效的自动化测试保留在各模块 tests/ 中；使用 `.venv/bin/python scripts/check.py [m1 m2 m3 m4]` 验证。

一次性验证脚本在任务完成后删除。
若具有长期排障价值，则整理到对应模块 scripts/；跨模块工具放根 scripts/。

### 文档

设计方案、规格和实施计划统一放在根目录 docs/。

禁止在各模块重复创建：

- docs/superpowers/
- plans/
- specs/

探索过程中产生的多个方案文档，任务完成后应合并为最终文档。

### 运行产物

所有：

- 分析结果
- 回测结果
- 预测结果
- Solver 输出
- Field trial 数据
- Debug 输出

统一写入：

outputs/<module>/<category>/<date-or-run-id>/

运行产物默认不提交 Git。

### 禁止

禁止为了记录工作过程创建大量：

- *_analysis.md
- *_investigation.md
- *_notes.md
- *_result.json
- *_debug.py

除非这些文件明确具有长期维护价值。

## 当前目录约定

- 当前 Git 与项目根为 `/Users/hua/Documents/LVK/code/vifa-en`；后续正式修改在本仓库完成，旧 `/Users/hua/Documents/LVK/code/vifa/vifa` 为迁移前保留副本。
- 源码按 `m1/`–`m4/` 归属；M3 Python 包为 `m3.worker`，M4 为 `m4.settings`、`m4.optimizer`、`m4.orchestrator`、`m4.selection`，不再创建旧顶层包。
- 设计、使用和部署文档按根 `docs/<module>/` 归档；不在业务模块内重建文档树。
- 根 `pyproject.toml` / `uv.lock` 共用；M3 Docker/Compose 位于 `m3/deploy/`，工具脚本在模块 `scripts/`。
- `.local/` 为本机配置和凭据，`runtime/<module>/` 为数据库、锁与运行状态；两者均不提交 Git。
- `outputs/m4/solver-decisions/` 是历史查询依赖的完整证据，不能按临时缓存删除。
- 完整迁移映射与基线验证见 `docs/项目目录与迁移.md`。
