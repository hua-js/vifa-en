# M3 默认预测日期修复

2026-09-07：电站 1 的 15 分钟最新任务生成于当天 10:37，但预测窗口为 9 月 8 日全天。原 latest 接口只按完成时间排序，导致这条未来任务成为默认结果，两条实际曲线均为空；5 分钟任务覆盖 9 月 7 日，因此能够显示实际值。

## 更新后的行为

- 默认结果按北京时间当天筛选：`forecast_start <= 当天零点 < forecast_end`，然后选取同电站、粒度、预测天数和模型策略下最近完成的任务。跨天的多日预测只要覆盖今天仍可入选。
- 没有覆盖今天的任务时，latest 接口返回原有 404 `not_found`；页面显示“今天暂无匹配预测结果”，清空曲线和摘要，不恢复历史或未来缓存，不回退到旧看板的拼接曲线。
- 页面跨日后的下一次可见刷新重新选择当天任务；明暗模式与原有每分钟刷新间隔保持不变。
- 手动预测仍可用于历史日期，完成后标明“历史预测”和完整起止时间。覆盖今天的结果标明“覆盖今天”；未来结果有独立日期标签，不作为默认结果展示。
- 历史截止时间是排他的零点。Worker 拒绝 `history_end > 今天零点` 的新请求，返回 422 `request_invalid`；页面历史结束日期（含当天）最晚可选昨天，并在输入越界时禁用提交。
- 不修改预测算法、数据表、采样或已有预测记录，不自动重新计算任务。

## 部署文件

Worker 更新以下两个文件并按现有流程重启服务：

- `m3_worker/services/custom_forecast_repository.py`
- `m3_worker/services/custom_forecast_service.py`

Node-RED 仅粘贴 `m3/node_red/m3_production_gateway_template.html` 到页面 Template 节点；本次不更新 Flow 文件。page.html 副本已删除，直接维护 Template HTML，不再运行旧同步脚本，见 `m3/node_red/README.md`。

本次仅完成本地实现与模拟接口验证，未部署、未修改线上任务。

## 必要验证

```sh
.venv/bin/python -m unittest tests.test_m3_current_day_forecast tests.test_m3_custom_forecast_repository tests.test_m3_custom_forecast_service tests.test_m3_daily_custom_forecast_service tests.test_m3_worker_api tests.test_m3_deployment_artifacts
node tests/test_m3_node_red_contract.js
node tests/test_m3_dashboard_auth_modes.js
node tests/m3_current_day_e2e.js
```

新增回归覆盖两站六种粒度、未来/历史任务排除、没有当天任务、多日窗口结束边界、UTC 时钟下的北京时间跨日、未完成历史日期拒绝、完成历史日期接受；页面覆盖未来任务防护、过期缓存、切站/切粒度、跨日刷新、手动历史查看，以及明暗模式和 1440/768/390/320px 布局。

以上为日期修复阶段的验证记录。随后按用户要求删除页面副本、撤回 Flow 改动；本次文件整理不运行测试、不调整旧测试或同步脚本，其中依赖 page.html 的脚本不再直接适用。
