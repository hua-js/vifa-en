# VIFA 当前交接

更新：2026-09-19。长期规则见 [AGENTS.md](AGENTS.md)，目录及正式页面见 [README.md](README.md)。本页仅保留当前口径、未结事项和证据入口；不作为生产版本或持续授权证明。

## 当前工作与状态

| 范围 | 已有实现／决定 | 待完成及证据入口 |
|---|---|---|
| M4 EMS联调 | 电站2建议功率严格大于10 kW才允许充放电，联调报文`kw`直接写600；不除柜数，不改原始建议。保留原需量/SOC校验、版本回读和失败暂停 | 新门槛及600映射的生产生效未核实；hold不得自动解除或重放旧计划。见[EMS接入](docs/m4/EMS对接材料.md) |
| M4滚动及展示 | 电站2凌晨谷电直接生成充电／待机，不等待预测或日计划；写表记录带m4_run_id和m4_plan_date，保留窗口外EMS记录；白天原门禁不变 | 当前工作区离线专项75项通过，日志：`outputs/m4/tests/2026-09-21-direct-night/unit-tests.log`；未部署或触发生产下发，未验证设备执行。见[自动计划](docs/m4/自动计划生成.md) |
| M3负荷／SOC | 原固定周排班作为缺失历史的日类型回退；SOC复用排班增量策略。临时加班表`production_overtime`已建，API与负荷/SOC接入已写入本地源码，未测试或部署 | 回放数据参与过排障，不能代替未来独立验证；生产是否含最新修复需核对。见[周周期设计](docs/m3/specs/2026-08-31-m3-weekly-load-forecast-design.md) |
| M3光伏 | 持续刷新训练数据、06:00/22:00双时段预测已在本地实现；M4读取保存的预测 | 训练更新与校正联合版本有高估退化记录，不能凭总体MAE改善直接上线；双时段需更新独立PV服务。见[光伏运行](docs/m3/光伏运行预测说明.md) |
| M3验收恢复 | 更新响应额外允许四个系统审计字段，仍严格校验业务字段、身份及值 | 尚无线上实际更新响应，不能认定唯一根因或已恢复；不重复生产写入。日志：`outputs/m3/tests/2026-09-19-acceptance-summary/` |
| M2持久化 | 已修复设备数值字符串及单记录upsert响应兼容；主表成功与事件补偿失败分开判断 | 生产事件写入和outbox恢复需另行核实。只读工具：`m2/scripts/check_bottleneck_health.py`；见[历史查询](docs/m2/历史查询说明.md) |
| M1 | 时间来源、历史告警及归属过滤已有实现；凭据从外部传入 | 生产状态未重验；新项目需真实上游及完整shared/config依赖。见[时间与历史告警](docs/m1/时间与历史告警说明.md) |
| NocoBase AI员工 | 用户已确认线上AI员工可用；关联权限插件0.1.2另属测试范围，仅保护独立验收角色 | 用户上传0.1.1后界面兼容检查失败；0.1.2补齐externalVersion元数据，本地官方检查函数和71项测试通过，待用户上传验收；正确业务库、真实页面及模型业务问答未验收。见[任务书](docs/NocoBase-AI员工实施任务书.md)1.7–1.10及[安装步骤](docs/NocoBase-关联权限插件生产安装.md) |

## 项目待办

以下两项由用户于2026-09-19提出，待实施：

- [ ] **M3生产排班接入**：已于2026-09-21按用户授权在NocoBase创建`production_overtime`（临时加班），字段及空列表已回读；支持电站多选、日期、全天/上午/下午/晚上、草稿/已确认/已取消、备注。普通排班使用生产API，86→ES01、113→ES02；API读取、临时记录合并及负荷/SOC接入已写入本地源码；两站加班时段08–12/14–18/19–21，全天为三段。排班密钥按用户要求已内置服务端，Worker新表只读权限待发布时核验；预测专项191项通过后用户停止本地验证，此次密钥默认值变更未重新测试，未部署或触发重算。见[生产排班接入](docs/m3/生产排班接入.md)。2026-09-21用户反馈电站2 SOC预测异常：9月20日周日加班、21日周一正常上班。只读查询确认当天15分钟任务`ab5df029-1534-4a45-9129-005758d69394`使用`SOCScheduleDelta`／`soc-schedule-delta-v2`，排班仍为`mon-sat-work-sun-rest-v1`；现有逻辑按“休息日→工作日”匹配，与实际“工作日→工作日”不一致。待支持排班例外并核对其对SOC历史样本选择及平台参考的影响；尚未量化本次曲线误差，未重算或写入生产。
  - 已按用户确认接入历史实测功率辅助SOC：每日/手动完整选型新增`SOCSchedulePower`，复用`emus_power`与接口容量`es_power_storage`，策略已进一步改为`soc-schedule-power-v2`：功率候选使用起报前7天、同日类别及排班时段的实测功率中位数，不再按初始SOC挑选功率形状；不新增数据库字段。只读接口已核对正放电/负充电和站点容量，已补充回归用例、静态审查，未运行本地测试或预测，未发布。短历史固定策略及旧常规入口尚不使用功率候选，详见生产排班接入说明末节。
  - 2026-09-21用户补充电站2排班（北京时间）：周一至周五正常班次为08:00–12:00、14:00–18:00、19:00–21:00；周六固定上午08:00–12:00上班，下午可能加班也可能不加班，晚间及周日按具体加班安排记录。不将该规则直接推广至其他电站。方案待定：从预测模型中抽离独立排班输入，支持时段级默认班次与加班例外，供负荷和SOC分别使用；通过加班后工作日的独立样本验证效果，不预先承诺精度改善。
  - 用户更新线上后，任务`20758fac…`已成功使用生产排班，但周候选沿用高起始电量历史增量，导致低SOC起点曲线失真。按用户确认，本地增加周候选起始SOC匹配门槛与实际预测窗口适用性预检，策略`soc-schedule-delta-v3`；保留排班基准及实测锚点。已补充回归用例并做静态代码审查，未运行本地测试、重算、构建或部署；此前191项结果不覆盖本次修正。详见生产排班接入说明末节。
- [ ] **全项目AI网关**：引入New API（newapi），统一管理和控制整个项目的AI网关。

## M4必须保持的业务边界

- 日计划是北京时间自然日96点比较；滚动从最新实测状态投影到下一刻钟，优化至日末；保留单站全天100元门禁，无净改善时使用同起点EMS模拟。未来负荷、评分与求解输入必须来自同一M3当前15分钟／1天任务，不跨任务拼接。真实负荷独立读取，不覆盖预测。
- 电站2凌晨谷电直接充电仅限零点连续谷段与固定EMS充电窗口交集；不依赖预测／日计划，依赖全柜实时SOC／功率、本站负荷、安全约束及写表回读；白天保持原门禁。不补发过去时段，下一刻钟生效。
- 固定EMS基线来自项目配置，执行计划单列；更新计划表不改变比较基线。电站2联调报文600是写表规则，不改成优化器功率上限，也不把基线功率改为600。
- Pyomo＋HiGHS保留三候选及独立校验。电站1费用优先，连续性策略v2；电站2保留需量／安全约束，连续性策略v3在原目标锁定后依次处理启动次数、谷段早充、平滑。参数按当前配置核对，不沿用旧试算数值。见[经济调度](docs/m4/求解器经济调度调整.md)。
- 光伏夜间20:00–次日07:00可补零，白天缺点阻断；上网价取当次`fprice`，仅12:00–14:00按经济性选择入储／上网，其余保留原策略。尖价用`jian/jprice`。见[光伏接入](docs/m4/光伏预测接入说明.md)。
- 光伏短时校正保留原始预测及证据；求解输入只采用向下校正。新增高估余量仅影子存证，未启用；不据历史回放声称线上收益已改善。
- 已结束时段默认显示日计划；仅匹配电站、run_id、日期、生效时间和时段的整版回读确认记录覆盖为滚动计划。业务状态可为“已下发·待生效／执行中／已执行”，仍不代表物理设备实测；日志只展示确认记录，原始建议保留在版本历史。
- 表写入部分失败／超时保留hold，不自动重试不确定写入。`t_model`“每天重复”的跨日清理、到期及整版原子激活尚未解决，用户明确暂不处理；恢复设备执行须另行授权验收。
- 自动计划默认开启；启动服务可能触发任务。页面“立即重算”是写操作，历史日期禁用，超时转查状态而非重放POST；旧UI移除要求不再适用。
- M4 AI决策和柜级分配页面/API已退役，不恢复；完整`outputs/m4/solver-decisions/`及既有历史证据保留。

## 发布与接入

- 一项目一实例，配置、运行和数据独立；入口为`VIFA_PROJECT_CONFIG`，不用同实例切换项目。M3网关自定义站点映射仍待补齐，不能宣称任意新站号已联通。见[产品化配置与交付](docs/产品化配置与交付.md)。
- M4入口`m4/deploy/build-and-push.sh`按白名单构建当前工作区，包含相关未提交／新增源码；仅推送固定`0.1.0-<architecture>`到腾讯CCR，Git基线和实际内容摘要单独存证。支持AMD64／ARM64，生产直接拉取。
- 默认无ZIP、无独立HTML副本、无Flow生成；临时构建目录用完清理，成功后只保留部署配置及发布记录。仅首次安装或网关变更时显式`--include-flow`打包或`sync_assets.py --flow --write`；纯HTML更新只用各模块正式源文件。详见[部署手册](docs/m4/deploy/部署手册.md)。
- 电站2生产更新必须保留三份Compose覆盖及`M4_EMS_STATION2_TABLE_WRITES=1`，具体约定见AGENTS.md。历史写入、SSH、推送授权不自动延续；本次整理没有生产操作。
- ARM64历史镜像已导入`192.168.1.53`，当时新容器被veth／mqueue能力阻断；未完成业务启动，M1现场Flow仍未取得。详见`outputs/m4/releases/product-arm64-reviewed/ARM64部署状态.md`；不据此连接主机。

## 验证与追溯

本次仅整理文档、生成入口与过时引用，未运行测试、构建或部署。既有专项测试只证明其当时版本和范围，不作为当前版本通过证明。需要发布时按AGENTS.md执行对应验证，检查前端、后端、网关的实际版本。

- 详细的9月14–19日过程、发布摘要和专项结果保存在清理前提交`ba452d4`的`CODEX_HANDOFF.md`：按关键词定位后用`git show ba452d4:CODEX_HANDOFF.md`读取对应片段，不默认展开全文。
- 更早记录见[项目交接历史](docs/项目交接历史.md)。`docs/*/plans/`是历史实施计划，旧指令、路径、测试状态不自动成为当前要求；仅需追溯时局部读取。
- 后续直接更新本页相应行，不在顶部累加全过程或复制日志。生产版本、配置和新任务结果须在具体任务内核实。
