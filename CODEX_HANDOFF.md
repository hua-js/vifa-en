# VIFA 当前交接

更新：2026-09-16。当前含 M1/M4 项目配置化与交付基础改造及 ARM64 文件交付；已获授权连接目标机，真实构建并导入镜像，但未启动新业务服务。项目规则见 [AGENTS.md](AGENTS.md)，目录与入口见 [README.md](README.md)。

## 当前状态与下一步

- M4 产品化配置及必需共享依赖纳入本次提交，M1 源码与测试改动仍留在工作区；Git 状态以实时检查为准。统一配置为 `shared/project.py`、`config/projects/{vifa,example}.json`，通过 `VIFA_PROJECT_CONFIG` 选择单项目；站点/设备/来源/既有策略贯穿后端、页面和 M4 Flow。配置版本参与当前计划失效判定，SQLite及证据目录绑定项目身份。详见[产品化配置与交付](docs/产品化配置与交付.md)。
- 用户确认整体目标覆盖 M1～M4：代码复用、项目配置不同、运行和数据各自独立，不做实例内项目切换。当前先处理 M4，保留已有 M1 改动；M2、M3 后续逐个推进，本轮不修改 M3。M4 本轮源码已改为动态站点健康检查，并增加 `M4_INSTANCE_NAME` / `M4_BACKEND_ALIAS` 部署配置，默认值兼容已有实例；已完成下述 AMD64 构建、推送和专项验证；后续源码清理的 M4 离线 Python 回归519项通过，但这些后续改动尚未构建/部署。M3 网关站点映射缺口仅记录待办，见[产品化配置与交付](docs/产品化配置与交付.md)。ARM64/AMD64 已支持打包与架构区分的发布脚本，不代表 ARM 真机或镜像构建验收。M1 凭据改为外部传入，部署须提供凭据和完整共享模块；新项目仍需核对真实上游数据与M3路由。
- 产品化审查发现的四处问题已修复并复核通过：M4名称校验、Flow Docker地址、M1旧历史首次归属与source_code精确匹配。最近历史检查包为 `product-arm64-reviewed.zip`，不包含本轮 M4 健康检查及部署配置修改；先前 `product-arm64-check.zip` 已失效。该包不代表当前源码或完整发布验收。
- ARM64文件部署：用户授权SSH到 `192.168.1.53` 后决定先完成文件部署。文件位于 `/userdata/holo/pyfiles/product-arm64-reviewed`；本机真实构建镜像 `vifa-m4:product-arm64-reviewed`，已传输校验并导入目标机，架构arm64。目标机新容器受veth不支持、mqueue挂载no such device阻塞；host网络/IPC未解决。未改内核、重启现有服务或导入Node-RED。M1现场Flow仍未提供。凭据已按用户指定文件安全落盘，不能将现场目录整体公开。记录：`outputs/m4/releases/product-arm64-reviewed/ARM64部署状态.md`。
- M4目前交付站级优化计划与展示，EMS负责柜间分配。尚无真实下发和执行回读，执行状态与策略日志为模拟，不能作为闭环或实测收益验收。
- 后续源码回归（2026-09-16）：已更新过时测试数据，清理7个废弃预测拼接/历史光伏/旧窗口准备用例；保留配置、并发、过期、选择和历史完整性校验，明确为历史兼容组件测试。新增5项真实当前任务→日计划→当天剩余时段的离线输入契约验证，不绕过评分校验。发现并修复后台决策API的残留 `prepared_inputs` 引用，改为复用 `get_inputs`。最终隔离 M4 Python 回归 **519项全部通过**，快照与工作区M4改动逐文件一致，`git diff --check`通过。详见 `/Users/hua/Documents/Codex/2026-09-16/mu-q/outputs/M4-过时测试清理说明.md` 与同目录全量日志。旧API/CLI及历史复验仍保留；测试通过不代表当前M3可提供从此刻起未来完整24小时预测。后续源码修复与测试清理纳入本次M4提交，尚未重建或部署。
- 原发布快照515项/45失败/65错误、中间快照520项/38失败/21错误均保留作历史证据，不再代表最新源码测试结果。AMD64镜像已推送，用户反馈生产与Flow已更新；未独立核验生产容器摘要、布局和完整执行日志，生产验收仍未完成。ARM64现场记录与AMD64生产状态分开核对。
- 下一步按用户任务推进：按用户后续要求提交或发布已验证源码，并核对生产配置、接口与布局；接EMS闭环前补当前状态起算、未来时段生效、旧计划替换及真实下发/回读。不要将这些待办视为本轮已获操作授权。
- AMD64发布（2026-09-16）：独立M4发布快照 `26cbafcd22e42a66a6651718c0367d2d7ba63938` 已构建并获用户明确授权上传 `ccr.ccs.tencentyun.com/taidai-holobase-168/vifa-m4:m4-26cbafcd22e4-amd64`；仓库摘要 `sha256:b42beb2ccee49b9c01e92f533765b2a05eaaa19d48014b7789717f5bbefe8bd9`，已核实linux/amd64。本轮61项专项、容器HiGHS求解/三站API/动态健康检查通过；隔离全量515项、45失败、65错误，未逐项归因，不能标记生产验收通过。交付方式为腾讯CCR镜像推送、生产直接拉取，不要求压缩包；M4 Flow JSON单独提供。用户反馈已更新生产和Flow、开启自动计划；17:12北京时间只读接口确认 `enabled=true`、15分钟周期，站2日计划17:09:45完成，站1旧计划失效，两站滚动建议过期。此为当时快照，未做浏览器布局验收，不代表后续窗口或设备执行已确认。完整日志与部署命令在 `/Users/hua/Documents/Codex/2026-09-16/mu-q/outputs/`。原工作区/分支保留，发布提交位于 `release/m4-amd64-20260916-165307`，未提交或发布M1本轮改动。
- 发布入口：[生产发布与接口缺口](docs/m4/deploy/生产发布与接口缺口.md)。

## M4 有效口径

- **固定基线发布后页面修复（2026-09-17）**：用户已更新后端；15:19只读核实站2新日计划完成、输入ready、基线为 `fixed-ems-636e5c1deeed7af78aa2`。页面仍比较实时执行controls版本且固定接受旧v7，误报配置变化；本地HTML已改用输入中的固定比较controls并校验基线来源，兼容v7/v8且核对请求/模拟版本一致，缓存v18。真实响应回放优化及EMS后备均96点通过，来源/安全版本拒绝与滚动刷新专项通过。需更新Node-RED的M4 HTML模板；后端无新改动，不需人为重算。未操作线上Template，未做浏览器视觉验收。
- **固定 EMS 基线（2026-09-17，本地未部署）**：用户确认按两站当前充放计划固化到项目配置 `stations[].ems_baseline`，电站2只把21:00:01改为21:00:00；电站1单柜充100/放60kW，电站2充100/放90kW，时段与来源见[配置说明](docs/m4/调度参数配置说明.md)。当前源无SOC字段，快照保存M4参考范围（1–99%、1–98%），始终叠加最新安全约束。日计划/滚动使用固定参考，执行计划单列，调试/下发不改比较基线；规划功率上限取已存设置与固定参考模式上限，实际设备能力仍待闭环接入。旧当前日计划失效、历史保留。窗口内失败60秒间隔、最多3次尝试，记录具体失败检查。发布需同步实际项目配置挂载文件；发布准备73项专项及保存数据的96点基线独立性检查通过，独立审查完成；本轮未部署、未线上重算或下发。
- **收益门禁（2026-09-14确认）**：两站全天优化计划相对同输入 EMS 基线的预计净节省（含两方案吞吐费用差）达到100元才可采用；不足100元沿用EMS，原安全、需量及日末电量校验继续保留。门禁版本 `daily-net-savings-100-v1`，旧版本当前计划要求重算，前端缓存升级v10并校验门槛。本次未运行测试、重算、构建镜像或部署。

- **M3负载来源（2026-09-14确认）**：M4预测负载、MAPE及求解输入统一使用M3当前15分钟/1天预测任务；实际负载从同一结果接口独立更新，保留缺失值，不覆盖预测。已移除滚动预测优先和跨任务补齐；旧混合来源日计划不再作为当前结果。源码完成，23项来源/MAPE专项测试通过；未部署或重算。当前任务不能覆盖的跨日未来窗口会阻断，不能拼接其他预测。

- **规划范围**：Pyomo + HiGHS，保留三候选及独立校验。当前页面主链为北京时间完整自然日、96个15分钟点、零点SOC起算的EMS基线与优化比较；不等同当前SOC起算的剩余日执行计划。历史实时链及95点窗口不替代日计划契约。
- **电站1日计划**：购电费用优先，需量偏差仅在同成本下择优；504 kW是当前控制参考而非计费门槛，550 kW是当前物理硬上限。SOC、设备功率、效率及日末电量约束保留。策略为 `station-1-cost-first-v1`；该目标顺序不扩展到电站2或实时候选。配置与来源版本仍须按任务核对。 用户已确认同样增加连续性择优，源码版本station-1-continuity-v1已于用户授权重启后本地重算：run 2e663e53-488d-43b4-9c9c-b4894b0746f5，费用4499.02元、节省35.06元；晚间放电至20:45，23:00–24:00仍放电，不能声称全部合并。较高优先级SOC偏好仍保留，未改其口径；未下发。 最新源码已按用户确认升级station-1-continuity-v2，将连续性置于SOC偏好之前、吞吐量之后；尚未重启/重算该版本，以上重算数据属于v1。
- **电站2日计划**：`peak-reserve-v3`，允许平段经济性放电，费用目标优先于首峰充满偏好，保留需量及安全约束。不以原EMS时段作为新优化计划的硬方向边界；旧方向字段仅供历史重放。不固定晚间放电终点。 当前本地已升级station-2-continuity-v2：原有目标锁定后先最小化放电启动次数，再平滑功率；未部署或生产重算。以下为历史v1证据：新增本地station-2-continuity-v1择优：原有目标之后、谷充时序之前最小化净功率变化，减少同成本孤立放电；2026-09-13用户授权重启/重算后，本地API PID24147（自动任务关闭），电站2run 7a7cc976-82c5-4388-b8bd-17e979bca482完成；晚间连续放电至20:15，22:45为待机。费用1193.59元、相对EMS节省46.02元，日末SOC34.72%；未跑完整测试、未下发。
- **设备与光伏**：ES01为emu11/12、无光伏；ES02储能为emu21～26，emu27为光伏计量。M4读取M3已保存光伏预测，页面读取不训练。2026-09-16确认光伏缺测仅北京时间20:00–次日07:00可补零，07:00–20:00缺任一点阻断日计划；已有值保留，无当日重叠仍阻断。新完整性版本`pv-night-zero-20-07-v1`使旧站2日计划退出当前方案，历史保留；页面标明日计划快照、生成时间及滚动建议入口。19项光伏/滚动专项测试通过，JS语法及快照提示检查通过；未部署、重算或布局验收。M3从06:15开始的预测，其当天00:00–06:15缺口已在允许补零范围内；07:00–20:00仍须完整覆盖。2026-09-16已按确认修改本地余电上网规则：有光伏余量时储能待机、余电全部上网，外送限制不足则阻断，不转充电；优化器、EMS模拟、独立校验及版本同步，19项优化器/光伏完整性/滚动专项测试通过，尚未部署。站2要求光伏先供负载、余电上网，但生产需核对保存的 `pv_export_priority`，前端模拟日志不能证明配置已生效。全天售电价当前为0。
- **滚动建议（2026-09-14实施，2026-09-16修复时间反序列化）**：每日首次成功生成日计划并执行单站全天100元门禁；每15分钟从最新实测SOC和储能功率投影至下一刻钟，仅优化至日末的剩余窗口。实测时效取配置上限与15分钟的较小者，越界/过期阻断。本轮读取M3实际负荷并存证，未来负荷仍用当前M3预测；2026-09-17用户确认的光伏短时校正已接入本地滚动链，见下项。门禁未通过或无净费用改善则提供同起点EMS模拟。滚动结果经`daily-plan.rolling`返回、独立落盘并有过期窗口；页面新增后续建议表，原日计划保留对照。用户已提供56ac62f镜像运行信息，但不代表滚动功能验收。2026-09-16根据98条datetime校验报错，修复日输入及剩余窗口JSON快照的严格反序列化；新增`m4.tests.test_m4_rolling_plans`，离线复现原错误后3项专项通过（剩余窗口及实测SOC起算、非法时间与数字字符串拒绝；MAPE门禁模拟）。本次修复未提交、构建或部署，未验证线上接口或求解器闭环。
- **光伏高估余量候选（2026-09-17，仅影子）**：更新模型加近期正误差余量的7日探索MAE62.77kW/正误差19.24kW，但当天2个新增目标MAE38.11→43.29、高估均0，独立证据不足。运行读取明确不启用新增余量，仅在`reserve_candidate_kw/reserve_kw`存证，现有求解输入保持向下偏差校正。校正版本v2、滚动版本v3，29项专项通过；未部署，联合发布继续暂缓。
- **光伏短时校正（2026-09-17，本地未部署）**：最近1小时闭合实测、至少3段持续偏差、±30%限幅，1小时完整修正后衰减至2小时归零。双向估计留档，滚动求解仅采用下调，避免光伏上调扩大充电空间；EMS基线同步输入。7日事件时间回放2212目标对，保守输入MAE74.16→68.41kW，平均正向误差36.14→26.82kW；单日MAE不保证改善，缺少历史到库时间，不能代替线上验收。48项相关用例通过，独立审查完成；原始预测/日计划/历史建议保留，未构建、部署或下发；详见[光伏接入](docs/m4/光伏预测接入说明.md)。
- **滚动历史展示（2026-09-16，本地未部署）**：`daily-plan.rolling_history` 从逐次归档还原当日已生效建议；新版本仅自其生效时间替代后续时段，过期窗口不延续为有效历史。前端图表/时间轴优先使用过去生效记录与当前有效后续建议，无滚动记录时保留日计划参考。调度日志按已生效建议的动作、功率及时间展示，与独立可展开的滚动版本记录分开；状态为未开始、进行中/已结束且未核实执行、已替代、已失效，不代表 EMS 已执行。独立滚动卡片隐藏。此轮仅做静态检查，未运行功能测试或部署。
- **自动生成**：后端默认开启，按15分钟窗口推进日计划及剩余日建议；`M4_AUTO_PLAN_ENABLED=0/false/off`可关闭。页面只读轮询，“立即重算”为调试入口。启动后端可能自动生成任务，不能把启动服务当成无副作用的只读检查；本地历史调试曾关闭，当前进程状态未核实。
- **展示**：紧凑无Hero布局，保留切站、图表、时段详情、经济摘要、调度表及右侧策略执行日志。当前SOC与实际功率取实测，计划SOC取所选计划时段末值；缺失或过期显示未知。执行状态按时间模拟，光伏日志使用独立示例数据。
- **退役功能**：柜级分配算法、预览/保存/查询API及页面入口已移除；不按旧交接恢复。既有 `solver-decisions` 和柜级分配历史证据未因退役删除。M4 AI决策已移除，不恢复旧AI链。

实现与说明：[经济调度](docs/m4/求解器经济调度调整.md)、[自动计划](docs/m4/自动计划生成.md)、[光伏接入](docs/m4/光伏预测接入说明.md)、[参数配置](docs/m4/调度参数配置说明.md)、[求解器决策](docs/m4/求解器决策说明.md)。

## M1–M3 状态

- **M3光伏持续训练（2026-09-17，本地未部署）**：用户确认训练数据持续更新；每日/手动forecast入口已接入光伏实测48小时重叠增量、历史天气更新及重新拟合。成功发布后原子保存训练源指针，失败阻断、不回退旧源。线上两批核对仍为8月18日至9月9日的2136点；49项相关用例通过，真实只读隔离刷新训练点增至2812、截止9月16日23:00；尚未构建或部署，不代表线上已更新。新模型加校正7日同样本回放MAE64.61kW（旧模型加校正68.41），平均正向误差29.31kW（旧26.82），存在高估退化，暂不建议直接发布联合版本。历史再分析天气有事后修订、遥测到库时刻未知，属于回溯评估。需`t_es_data:list`及历史天气网络访问；发布准备验证见[光伏运行预测](docs/m3/光伏运行预测说明.md)。

- **M1**：时间来源、SQLite历史告警及站/设备归属过滤已有实现。部署须同步页面与Python，保留持久化数据；本次未重新核实生产状态。见[时间与历史告警](docs/m1/时间与历史告警说明.md)。
- **M2**：历史效率及瓶颈查询已有实现；部署须同步HTML、Python与Flow，本次未重新核实生产状态。见[历史查询](docs/m2/历史查询说明.md)。
- **M3/M4评分（2026-09-14，代码待发布）**：用户确认由M3后端统一计算夜间加权评分，M3页面与M4读取同一 `current_score`；M4准入改用该评分，门槛仍为30%。保留标准MAPE原有统计、预测值、选模及SOC。当前评分随GET实际值回填计算，正式评估时保存到评估表新增可空JSON字段；GET不写库。先新增字段、发布M3 Worker和Template，再发布M4，缺字段时阻断。尚未测试、迁移或部署；字段和发布步骤见[部署说明](docs/m3/部署说明.md)。
- **M3日期**：历史表单开始日期保持、结束跟随昨天，最多90天，按站保存；旧任务响应不回退表单。仅维护生产Template HTML，不同步旧副本或Flow JSON。
- **已取消实验**：夜间预测校准代码已按用户要求丢弃，不再是未完成待办，也不自动恢复。
- **光伏与部署**：M3已有天气采集、光伏预测入库及每日06:00生成能力；M4只查询结果。历史OOM修复、Worker内存限制和各版本生产状态需发布任务中核实，不凭历史记录认定已部署。见[光伏运行预测](docs/m3/光伏运行预测说明.md)及[部署说明](docs/m3/部署说明.md)。

## 已有验证与限制

| 范围 | 最近保存的证据 | 限制 |
| --- | --- | --- |
| M1/M4 产品化专项 | M1配置/历史23项；共享配置/M4三站与隔离10项；M3读取18项与网关4项；前端/Node-RED36项，均离线通过；打包、发布与包外配置加载测试通过 | 该行是历史专项证据，最新M4 Python回归见下行。无真实上游、Node-RED导入、浏览器布局及ARM真机验收 |
| 联合检查包 | `outputs/m4/releases/product-arm64-reviewed.zip`；95条校验和全部匹配，ZIP完整性通过；配套 `release.json`、`SHA256SUMS`；生成资源检查 `stale: []` | 当前工作区源码检查包，非正式已发布镜像，未部署 |
| M4全量 | 测试清理后的工作区隔离Python回归519项全部通过；原515项及中间520项失败日志保留，见过时测试清理说明 | 历史兼容组件测试与现行输入验收已分开；纳入本次M4提交，尚未构建/部署，不等于生产及浏览器验收 |
| M4前端 | 同目录：模拟GET下时间字形、两站切换、滑块、PV指标、历史入口；12组尺寸/主题无整页溢出 | 仅模拟接口；未覆盖凌晨空日志以外的日志内容，不能代替真实接口验收 |
| M4打包 | `outputs/m4/releases/20260912-release-readiness`：恢复8个backend文件，79文件打包及6项发布专项通过 | 历史准备包，非当前正式发布物；未因此消除其他测试失败 |
| M3 | 评分专项和当前日E2E有通过记录 | 完整dashboard E2E历史存在旧文案等待超时，不能声称全量通过 |

历史前端验证使用过无界面Chrome；用户现已禁止调用Google Chrome，后续不复用该浏览器方式。验证时机与范围以AGENTS.md及当前用户要求为准。

## 本地与发布入口

- 最近本地调试入口为 `127.0.0.1:8847/m4`，代理8848；2026-09-13记录为8847未响应，**当前是否运行未核实**。旧8844及历史PID不作为当前启动状态。
- 调试工具：`m4/scripts/local_debug_api.py`、`m4/scripts/preview_console.py`。历史 `--skip-mape` 仅用于明确授权的本机调试，不带入生产、不改变生产门槛。按需读脚本和发布文档，不为检查文档启动环境。
- M4 构建推送入口 `m4/deploy/build-and-push.sh` 从干净Git提交导出，支持 `linux/amd64` / `linux/arm64`，使用架构区分的提交标签。默认通过腾讯CCR镜像仓库交付，生产直接拉取；HTML/Flow如有变更单独同步，不要求压缩包。新的挂载、标签和联合包规范见[产品化配置与交付](docs/产品化配置与交付.md)；历史固定 `0.1.0` 标签不代表本轮代码。本次AMD64唯一标签已获授权推送并核验摘要。历史生产目录 `/userdata/holo/pyfiles/m4`，Node-RED需与后端共享网络；实际部署前核对现场入口与配置。
- 本次获M4相关提交、AMD64构建及指定腾讯CCR标签上传授权；生产部署由用户执行，代理仅做已授权API只读查询。没有本轮SSH、写API或设备控制授权；后续操作仍按对应权限执行，保留全量失败与真实集成待验收项。

## 历史追溯与维护

- 整理前1040行原交接归入[项目交接历史](docs/项目交接历史.md)，只在需要具体日期、run_id、旧试算或发布证据时检索；其中“当前/最新/未提交”均为历史措辞。
- 历史一次性告警豁免、自动化审批、部署授权不延续为当前权限；当前参数、服务和发布状态以任务中的实际证据为准。
- 后续更新本文件对应段落，替换失效状态并保留必要证据链接，不继续向顶部累加全过程日志。
- 项目续接及文档维护使用 [vifa-project-handoff](.agents/skills/vifa-project-handoff/SKILL.md)。
